# 2026-07-30 세션 18 — 11차 게이트 (코드 수리 성립·의존성 한 겹) → 수리

**도구**: Claude Code (Claude Fable 5) + Codex (gpt-5.6-sol xhigh, 러너 에이전트 경유)

## 11차 판정: FAIL (P1 0 · P2 1 · P3 1) — 전문 = audits 문서 §11차

처음으로 **런타임 로직에서 새 누수가 나오지 않음** — 10차 수리 4건 전부 실측 성립 (close 변종 1.04초 회수, 견고성 8케이스 무누출). 남은 건 의존성 선언 한 겹.

## ⚠️ 오답 16호 — 하한 선언을 기능 도입 버전과 대조하지 않았다

`urllib3>=2.0`이라 선언하고 "실검증 2.7.0"이라 주석까지 달았지만, **그 사이 버전들을 안 봤다**. 러너가 휠 실물을 버전별로 내려받아 대조: read1은 2.2.0, shutdown()은 2.3.0 도입 → **2.2.x는 read1만 검사하는 내 임포트 가드를 통과하면서 shutdown이 없어** 3라운드 걸려 막은 누수가 조용히 부활. 그 환경에선 레포 자체 회귀 테스트 3개가 FAIL — 즉 레포가 스스로 허용한 조합에서 스스로 깨진다. 교훈: **검증 축은 변종(15호)만이 아니라 버전 연속선에도 있다** — 하한 선언은 "기능이 언제 생겼나"와 대조해야 성립한다.

부수 기록: Codex의 두 렌즈가 갈림 (exec=PASS, review=P1 주장·전제 일부 오류) — 러너가 실물 검증으로 중재해 P2 확정. 검사자도 교차 검증 대상이라는 구조가 또 작동.

## 수리 내역

1. P2-1: `urllib3>=2.3` 하한 + 임포트 가드에 shutdown 검사 추가(read1과 동일 fail-fast) + docstring 정정(위험 창=2.2.x·폴백=keep-alive 한정 이중 방어)
2. P3-1(델타 밖 기존 결함, 즉시 수리): JSON `null`이 `.get(기본값)`을 우회해 매 체크 TypeError — 선택 필드 4개 전부(클래스) 검증에서 null 명시 거부 + 회귀 테스트. 음성대조: 수리 전 config로 되돌리면 정확히 RED

검증: 40개 5연속 GREEN · 음성대조 RED→GREEN 실측 · 라이브 🟢

## 적용 메모리/규칙

증상 vs 클래스(P3-1을 필드 4개 전수로) · 의존성 하한=기능 도입 버전 대조 · 검사자 판정도 실물 재검증 후 수용 · crash bug=즉시 fix
