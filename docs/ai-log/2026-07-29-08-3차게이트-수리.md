# 2026-07-29 세션 08 — 3차 게이트 (P1 첫 0건) → 잔존 P2 5건 수리

**도구**: Claude Code (Claude Fable 5) + Codex (gpt-5.6-sol xhigh)

### 추세

P1 4 → 1 → **0**. 1·2차 지적은 전부 실측 종결 확인. 잔존은 열화 경로 P2 5건 (상세=`docs/audits/2026-07-29-codex-gate-D1.md` 3차 섹션).

### ⚠️ 오답 5호 — 검사자(게이트 러너)의 권고 자체가 틀렸던 사례

2차에서 러너가 권고한 "콘솔 폴백도 전달로 간주"(N4 처방)가 **반쪽 설정(토큰만 있고 챗ID 오타) 시 텔레그램에 아무것도 안 가는데 '전달 성공'으로 기록되는 갭(G5)**을 만들었고, 3차에서 러너가 이를 스스로 적발·시인함. 작성자만이 아니라 **검사자의 판단도 다음 라운드의 검증 대상**이라는 것 — 교차 게이트를 반복 라운드로 도는 이유가 이것.

### 사람 몫 결정 2건 (게이트 질의 → 내 판단·근거)

- G1: 독스트링 정정만 vs 코드 구현 → **구현** (미통보 순단이 조용히 사라지는 건 감시 도구의 정직성 문제. "순단 후 자가 복구" 사후 보고 1회)
- G6: 부분 수신을 FAIL 유지 vs WARN 강등 → **WARN** (🔴의 신뢰 보존 — 느린 것과 죽은 것은 다른 등급)

### 수리 + 검증

- G1~G5 + P3 전량 수리. G3은 문서로 때우지 않고 스레드 데드라인으로 실제 강제
- G8 교훈 반영: **실경로 통합 테스트** — 진짜 찔끔 서버(0.5초 간격 800바이트)를 띄워 총 시한 2초에 실제로 끊기는지 실측. 직접 호출 분기 검증만으로는 G3을 못 잡았다는 지적 수용
- 스위트 22→28개 GREEN (처음엔 50초 걸리던 것을 서버 스레드 데몬화로 2.0초로 교정 — 이것도 테스트 품질 결함으로 기록)
- 라이브: status·once 3층 GREEN 유지

### 적용 메모리/규칙

검사자 판단도 재검증 대상 · 가드는 실제로 깨보고 증명(실경로 테스트) · 알림 등급=신뢰 자원 · 게이트 반복=P1/P2 0까지
