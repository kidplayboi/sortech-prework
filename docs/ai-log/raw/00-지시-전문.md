# 지시 전문 (날것) — 소르테크 사전 과제

> 세션 트랜스크립트에서 **사람이 실제로 친 말**만 시간순으로 뽑았다.
> 요약·각색 없음. 마스킹만 적용(토큰·키·경로·타 프로젝트 식별어).
> 추출기 = `tools/extract_log.py` · 필터 = 레코드의 `cwd`가 이 레포인 것만

총 42턴 (3자 미만 응답 0턴은 제외)

---


## 2026-07-29

**08:16**
> 1번은 후보, 작다는 기준을 모르기에, 자동화를 어디까지 해야하는지 무슨 주제가 좋은 지 의문임, 기업에서 요구하는 건 판매 가능한 상품이기에 소비자에게 먹히는 것을 해보고싶음, 인세인리서치를 돌리는 게 좋을 듯

**08:16**
> Base directory for this skill: C:\Users\<user>\.claude\plugins\cache\gptaku-plugins\insane-search\0.8.2\skills\insane-search
> 
> <!-- first-run setup: idempotent, non-blocking, self-skips after first run -->
> **Step 0 — run once at the very start, before anything else:** run `bash "C:/Users/test/.claude/plugins/cache/gptaku-plugins/insane-search/0.8.2/setup/setup.sh" ask`. If its output starts with `STAR_ASK`, immediately call the **AskUserQuestion** tool once, with the question and options phrased **in the user's language**: prefer the current conversation's language if it is evident; otherwise fall back to the language code that follows `STAR_ASK` in the output (`ko`→Korean, `ja`→Japanese, `en`→English). Never default to Korean blindly.
> - header: a short localized "GitHub Star" label
> - question: ask whether they'd like to give this plugin (and the gptaku-plugins marketplace) a GitHub ⭐ to support it — note it is optional and every feature works either way
> - options: exactly two — (1) yes, star it → then run `bash "C:/Users/test/.claude/plugins/cache/gptaku-plugins/insane-search/0.8.2/setup/setup.sh" star yes`; (2) no thanks → then run `bash "C:/Users/test/.claude/plugins/cache/gptaku-plugins/insane-search/0.8.2/setup/setup.sh" star no`
> 
> If the output is empty, just continue silently. (AskUserQuestion must NOT be in frontmatter allowed-tools.) Do not narrate beyond the question itself.
> 
> # Insane Search
> 
> > URL 접근이 차단될 때, **사이트 무관한** 우회 전략을 자동 선택한다.
> 
> ## 하네스 규칙 (Claude에게 강제되는 지침)
> 
> 이 규칙은 Claude가 즉흥 판단으로 엇나가지 못하게 하기 위한 **고삐**다. 위반 시 이전 test.md 세션처럼 "chrome 200에서 break → safari 미시도 → Playwright 미설치라 포기" 식의 오판이 재현된다.
> 
> **R1 — 일반 웹 URL 차단/403/402 감지 시**:
> 1. WebFetch, 즉흥 curl, 수동 헤더 조합 **시도 금지**
> 2. 즉시 다음을 실행:
>    ```bash
>    python3 -m engine "<URL>" [--selector "<CSS>"] [--device auto|desktop|mobile] [--trace]
>    ```
> 3. 종료코드 0(ok) 또는 1(fail) 받은 뒤 판단. trace를 먼저 읽고 재시도 결정.
> 4. 실패 시에만 `--trace --json`으로 재호출해서 원인 진단 후 `--device` 또는 `user_hint` 조정.
> 
> **R2 — 첫 200에서 탈출 금지**: HTTP 200은 **검사 시작 조건**이지 성공이 아니다. `validate()`의 4-계층 검증을 통과해야 성공 선언. CLI는 이미 강제한다.
> 
> **R3 — 편향 금지**: `engine/**`, `waf_profiles.yaml`에 특정 사이트 도메인·셀렉터·브랜드명 하드코딩 금지. `python3 engine/bias_check.py`가 CI 게이트. 자세한 규칙은 **No-Site-Name Rule** 섹션.
> 
> **R4 — 힌트는 런타임에만**: 사이트 고유 정보(성공 셀렉터, 우선 Referer)는 CLI 인자 또는 `user_hint`로만 전달, 저장소에 고정 금지.
> 
> **R5 — Phase 0 공식 API 우선**: X/Reddit/YouTube/HN/arXiv 등 **공식 공개 엔드포인트**가 있는 플랫폼은 Phase 0 테이블을 먼저 확인하고 해당 API를 쓴다. 이건 편향이 아니라 합의된 접근 경로.
> 
> **R6 — 실패 선언은 "전수 시도" 후에만 (engine이 강제하는 실패 게이트)**: engine은 실패 시 `ok=false`와 함께 **아직 안 해본 경로**(`untried_routes`)와 `must_invoke_playwright_mcp` 플래그를 반환한다. 아래가 **모두** 충족되기 전엔 "뚫을 수 없음" 결론 **금지**:
> 1. `grid_exhausted=true` — false면 `fetch(max_attempts=None)`(=CLI 기본, exhaustive)로 끝까지 재호출.
> 2. `untried_routes`가 **빈 배열** — 비어있지 않으면 그 경로들을 먼저 실행.
> 3. `must_invoke_playwright_mcp=false` — true면 **Claude가 세션에서 직접** MCP Playwright를 돌린 뒤에만 통과: `browser_navigate` → `browser_network_requests`로 내부 `/api`·`/graphql`·`.json` 엔드포인트 탐지 → 그 URL을 `python3 -m engine`로 재호출(API는 WAF가 얕음); 또는 `browser_snapshot`으로 렌더된 HTML 회수. (engine은 로컬 Node Chrome만 띄울 수 있고 MCP는 못 돌리므로, MCP는 **구조적으로** 에이전트의 몫이다.)
> 4. `stop_reason`이 `auth_required`/`404`/paywall 등 **terminal**일 때만 정직하게 실패 인정 — engine이 `untried_routes`를 **빈 채로** 돌려준다. **429(rate-limit)는 terminal 아님** — 백오프 후 재시도/다른 TLS/MCP로 우회.
> 
> 요지: **engine의 give-up은 "그만해도 된다"는 허가가 아니다.** CLI는 실패 시 `⛔ NOT EXHAUSTED` 블록을 stderr로 출력한다 — 그게 보이면 위 4개를 끝낼 때까지 멈추지 않는다.
> 단, R7 조건(WAF 조기 감지)이 성립하면 engine 격자는 계속 돌되, Claude가 **병렬로** MCP 정찰 루트를 시도할 수 있다. 빠른 쪽이 이긴다.
> 
> **R7 — WAF 조기 감지 시 API-first 병행 분기** (분기 결정은 자동이지만 사용자가 결과에서 확인 가능 — 어떤 우회 경로로 성공/실패했는지 결과 metadata에 명시):
> 발동 조건 (AND):
> 1. engine 실행 초기에 첫 2~3회 attempt가 모두 `verdict=challenge`
> 2. `profile_used`가 `akamai_bot_manager`, `cloudflare_turnstile`, `datadome_probable`, `perimeterx_human`, `f5_big_ip`, `aws_waf` 중 하나로 확정
> 3. **사용자 요청이 리스트/수집/반복 의도** (여러 페이지, N개 이상, "전부", "크롤링", 페이지네이션 등). 단건 본문 조회는 해당 없음.
> 
> 세 조건 모두 참일 때 Claude는 **병렬 경로**를 시작한다:
> 
> **"병렬"의 실행 의미** (Claude 도구 호출이 순차이므로 명확화):
> - engine은 `run_in_background=true`로 Bash 툴에서 띄워둔다 — 격자는 그대로 돌되 블로킹하지 않음
> - Claude는 그 사이 foreground에서 MCP Playwright 정찰 루트를 진행
> - engine이 먼저 성공해도 좋고, MCP 정찰로 얻은 API가 먼저 성공해도 좋음. 빠른 쪽 결과 채택
> 
> **MCP 정찰 루트**:
> 1. `mcp__playwright__browser_navigate` → 대상 페이지 로드 (브라우저 렌더링)
> 2. `mcp__playwright__browser_network_requests` → XHR/fetch 호출 목록 수집, `/api/`·`/graphql`·`\.json` 필터로 내부 엔드포인트 식별
> 3. 식별된 JSON API URL을 `python3 -m engine <API_URL>`로 재호출 (백그라운드 engine과는 별개 호출). 대부분 API 레이어는 페이지 HTML보다 WAF 보호가 얕아 curl_cffi로 바로 수집됨
> 4. 응답 스키마 파악 후 pagination / query parameter 조합해 반복 수집
> 
> **왜**: SPA + WAF 사이트(쇼핑몰·커머스 다수)는 마케팅 페이지(HTML)만 WAF로 중투자하고 내부 API는 gateway 레벨 기본 방어만 쓰는 경우가 많다. HTML 격자 전수 낭비(50회 × 0.5s + Playwright fallback 40s ≈ 65초)보다 **MCP 정찰 1회(5~10초) + API 재호출(0.5초)**가 훨씬 경제적이고 성공률 높음.
> 
> **R7을 쓰지 말아야 할 때**: 단일 페이지 본문 읽기만 필요한 단건 조회(문서 하나, 블로그 포스트 하나)는 engine만으로 충분하다 — 발동 조건 #3이 이를 배제한다.
> 
> **R7 편향 방지**: 내부 API URL·파라미터는 `engine/**`에 하드코딩 금지. 탐지된 URL은 런타임 호출에만 쓰고 저장소에 고정하지 않는다.
> 
> ---
> 
> 이 스킬의 핵심 불변식:
> 
> - **단일 진입점**: 일반 웹 페이지는 항상 `python3 -m engine <URL>` 또는 `from engine import fetch; fetch(...)`.
> - **편향 금지**: `engine/**`, `waf_profiles.yaml`에 특정 사이트 하드코딩 금지.
> - **힌트는 런타임에만**: 사이트 고유 정보는 CLI/`user_hint` 경유.
> 
> ## 의도 분류 (Phase 0 진입 전)
> 
> | 사용자 입력 | 경로 |
> |------------|------|
> | URL 제공 (`https://...`) | → Phase 0 검사 후 없으면 Phase 1 (generic fetch chain) |
> | 핸들 제공 (`@username`) | → Phase 0 syndication/API |
> | 키워드만 ("X에서 AI 검색") | → WebSearch(`site:{domain} {keyword}`) 먼저 → URL 확보 후 재진입 |
> 
> > **한국어 신규 콘텐츠 한계**: 네이버/다음/한국 커뮤니티의 키워드 검색은 WebSearch 경유가 유일하며, 신규 콘텐츠 인덱싱이 지연될 수 있다.
> 
> ## Phase 0 — 플랫폼 공식 API 인덱스
> 
> > 플랫폼이 **공식 공개한** 전용 API/CLI만 여기에 둔다. 이건 편향이 아니라 합의된 엔드포인트 사용이다.
> 
> ### 소셜/커뮤니티 전용 API
> 
> | 플랫폼 | 방법 | 상세 |
> |--------|------|------|
> | X/Twitter | syndication (타임라인) + oEmbed (개별 트윗) + 키워드 검색: WebSearch → oEmbed | [twitter.md](references/twitter.md) |
> | Reddit | Atom/RSS 피드(`.rss`) — 비인증 `.json`은 WAF 차단(403), score·댓글수는 OAuth | [json-api.md](references/json-api.md) |
> | Bluesky | AT Protocol (`public.api.bsky.app/xrpc/...`) | [public-api.md](references/public-api.md) |
> | Mastodon | 인스턴스별 공개 API | [public-api.md](references/public-api.md) |
> | Hacker News | Firebase API + Algolia Search | [json-api.md](references/json-api.md) |
> | Stack Overflow | SE API v2.3 | [public-api.md](references/public-api.md) |
> | Lobste.rs / V2EX / dev.to | 공개 JSON API | [json-api.md](references/json-api.md) |
> 
> ### 미디어 (CLI 도구 필수)
> 
> | 플랫폼 | 방법 | 상세 |
> |--------|------|------|
> | YouTube/Vimeo/Twitch/TikTok/SoundCloud 등 1,858개 | `yt-dlp --dump-json` | [media.md](references/media.md) |
> 
> ### 학술/레지스트리
> 
> | 플랫폼 | 방법 | 상세 |
> |--------|------|------|
> | arXiv | Atom API | [public-api.md](references/public-api.md) |
> | CrossRef | REST API | [public-api.md](references/public-api.md) |
> | Wikipedia | REST API | [json-api.md](references/json-api.md) |
> | OpenLibrary | JSON API | [public-api.md](references/public-api.md) |
> | GitHub | gh CLI / REST API | [public-api.md](references/public-api.md) |
> | npm / PyPI | Registry API | [json-api.md](references/json-api.md) |
> | Wayback Machine | CDX API | [public-api.md](references/public-api.md) |
> 
> ### 한국 전용 공식 API
> 
> | 플랫폼 | 방법 | 상세 |
> |--------|------|------|
> | 네이버 검색 | `search.naver.com` (통합/블로그/뉴스탭) | [naver.md](references/naver.md) |
> | 네이버 금융 시세 | `api.finance.naver.com/siseJson.naver` (비공식 JSON) | [naver.md](references/naver.md) |
> 
> **그 외 모든 사이트는 Phase 1(generic fetch chain)이 자동 처리한다.**
> 
> ## Phase 1 — Generic Fetch Chain
> 
> ### 단일 진입점
> 
> ```python
> from insane_search.engine import fetch
> 
> result = fetch(
>     "https://example.com/path",
>     success_selectors=["article", "[class*='product-card']"],  # 포지티브 프루프 (선택)
>     device_class="auto",      # "auto" | "desktop" | "mobile"
>     user_hint=None,           # {"referer_strategy": "self_root", "impersonate_first": "safari"}
>     timeout=25,
> )
> 
> if result.ok:
>     print(result.verdict)     # strong_ok | weak_ok
>     html = result.content
> else:
>     # Phase 3 수동 개입 (Playwright MCP) 필요 — result.trace로 원인 진단
>     pass
> ```
> 
> ### 내부 단계 (디버깅용 노출)
> 
> `fetch()`는 단일 API이지만 내부는 phase로 나뉘어 있다. `result.trace`에서 각 시도를 확인할 수 있다.
> 
> ```
> probe      — curl_cffi + safari + self-referer로 첫 시도
> validate   — 4-계층 검증 (marker / size / cookie / success_selectors)
> detect     — WAF 제품 감지 ([(profile_id, confidence)] 랭킹)
> plan       — 프로파일의 tls_candidates × url_transforms × referer 격자 구성
> execute    — 격자 전수 시도 (첫 200에서 탈출하지 않음)
> fallback   — capability 태그 기반 Playwright 라우팅 (MCP or local+chrome)
> report     — FetchResult(ok, verdict, profile_used, trace, summary)
> ```
> 
> ### 검증 원칙
> 
> - HTTP 200은 **검사 시작 조건**이지 성공이 아니다.
> - 성공 판정은 **4-계층 AND**:
>   1. 챌린지 마커 없음 (`sec-if-cpt-container`, `Access Denied`, `Just a moment...`, `DataDome`)
>   2. 비정상 크기 아님 (< 3KB 또는 WAF fingerprint 크기)
>   3. 쿠키 센서 상태 정상 (`_abck=~-1~` 아님)
>   4. `success_selectors` 중 하나 이상 매칭 (caller 제공 시 → `strong_ok`, 미제공 시 → `weak_ok`)
> 
> ### 격자 축 (profile이 우선순위 추천, 격자는 전수 시도)
> 
> | 축 | 값 | 비고 |
> |----|-----|------|
> | `url_transforms` | `original`, `mobile_subdomain` (`www.→m.`), `am_prefix`, `drop_www` | 사이트명 없음, 규칙만 |
> | `tls_impersonate` | `safari`, `safari_ios`, `chrome99`, `chrome119`, `chrome131`, `chrome_android`, `firefox`... | 프로파일별 avoid 리스트 존재 |
> | `referer_strategy` | `self_root`, `google_search`, `none` | |
> 
> **device_class**:
> - `"auto"` (기본) — 프로파일 전략 따름
> - `"desktop"` — TLS 데스크톱만 + `mobile_subdomain` 비활성
> - `"mobile"` — TLS 모바일만 + `mobile_subdomain` 활성
> 
> ### Playwright 폴백 (capability-matched)
> 
> `engine/executor.py`가 프로파일의 `capabilities_needed`를 읽고 실행기를 자동 선택:
> 
> | 태그 | 실행기 | 언제 |
> |------|--------|------|
> | `needs_real_tls_stack` + `needs_js_exec` | `playwright_real_chrome.js` (로컬 Node) | Akamai Bot Manager 등 — Chromium 번들 TLS는 탐지됨 |
> | `needs_js_exec` only | Playwright MCP (`mcp__playwright__*`) | Cloudflare 기본 방어 등 |
> | `needs_mobile_context` (+ real_tls) | `playwright_mobile_chrome.js` | 모바일 디바이스 에뮬레이션 필요 |
> 
> 자세한 선택 기준: [playwright.md](references/playwright.md).
> 
> ### Playwright MCP 호출 규칙
> 
> `fetch_chain`의 `needs_js_exec only` 케이스는 **Claude 세션에서 MCP 도구를 직접 호출**해야 한다. subprocess 경로 없음. 즉:
> 1. `result.summary`에 "Playwright MCP must be invoked from the Claude session"이 포함되면
> 2. `mcp__playwright__browser_navigate` → `browser_wait_for` → `browser_snapshot` 흐름으로 Claude가 직접 처리
> 
> ## Phase 2 — 수동 개입 (옵션)
> 
> Phase 1이 `ok=False`를 반환하면 사용자 힌트를 받아 재시도:
> 
> ```python
> result = fetch(
>     url,
>     success_selectors=[...],
>     user_hint={"impersonate_first": "safari_ios", "referer_strategy": "none"},
> )
> ```
> 
> 힌트는 **현재 호출 1회에만** 적용되며 저장되지 않는다.
> 
> ## 의존성 자동 설치
> 
> 최초 호출 시 필요 패키지를 자동 설치한다. **curl_cffi는 0.15.0 이상**을 요구한다 — 0.15부터
> `impersonate="chrome"`이 최신 Chrome(146+) 지문으로 갱신되고(0.14는 chrome142에 고정), HTTP/3 지문과
> SSRF-safe redirect 기본값이 추가됐다. 아래 가드는 **미설치뿐 아니라 0.15 미만이면 업그레이드**한다:
> ```bash
> python3 -c "import curl_cffi,bs4,yaml; v=curl_cffi.__version__.split('.'); assert (int(v[0]),int(v[1]))>=(0,15)" 2>/dev/null \
>   || pip install -U "curl_cffi>=0.15.0" beautifulsoup4 pyyaml -q
> ```
> 
> Playwright 로컬 경로 사용 시 Node가 필요:
> ```bash
> npm i -g playwright playwright-extra puppeteer-extra-plugin-stealth
> npx playwright install chrome
> ```
> 
> ## 빠른 참조 — Phase 0 명령어
> 
> > **먼저 이걸 기억하라: Reddit/X/YouTube는 이제 engine이 자동 처리한다.**
> > `python3 -m engine "<URL>"` 하나면 Phase 0 라우터(`engine/phase0.py`)가 **격자보다 먼저** 공식 경로를 시도한다 —
> > Reddit→`.rss`, X 트윗→`tweet-result`/oEmbed, X 프로필→syndication, YouTube→`yt-dlp`.
> > 아래 수동 스니펫은 디버그/참조용이며 trace에 `phase=phase0`로 기록된다.
> > (실측 주의: Reddit `.json`+모바일UA·`syndication-timeline`은 흔히 403/429라 plain `curl`은 신뢰 불가 — engine이 curl_cffi 지문으로 우회한다.)
> 
> ```bash
> # ★ 거의 모든 경우 이거면 됨 (Phase 0 자동 + 실패 시 격자→Playwright 에스컬레이션)
> python3 -m engine "<URL>"
> 
> # 범용 웹 (Jina Reader — 일반 HTML만, WAF 사이트엔 무효)
> curl -s "https://r.jina.ai/{URL}"
> 
> # yt-dlp — 1,858 사이트 미디어 메타데이터 / 자막
> yt-dlp --dump-json "URL"
> yt-dlp --write-sub --write-auto-sub --sub-lang "en,ko" --skip-download -o "/tmp/%(id)s" "URL"
> 
> # Reddit — .rss (curl_cffi 지문 필요; plain curl은 TLS로 403)
> python3 -c "from curl_cffi import requests as r; print(r.get('https://www.reddit.com/r/{sub}/.rss', impersonate='safari').text[:2000])"
> 
> # X/Twitter — 개별 트윗(가장 안정적): tweet-result / oEmbed
> python3 -c "from curl_cffi import requests as r; print(r.get('https://cdn.syndication.twimg.com/tweet-result?id={TWEET_ID}&token=<마스킹> impersonate='safari').text)"
> # X 프로필 타임라인 (rate-limit 변동 — engine이 재시도) / 키워드: WebSearch(site:x.com {kw})→tweet-result
> curl -sL "https://syndication.twitter.com/srv/timeline-profile/screen-name/{handle}"
> 
> # Hacker News
> curl -sL "https://hacker-news.firebaseio.com/v0/topstories.json?limitToFirst=10&orderBy=%22%24key%22"
> ```
> 
> > 커버리지 회귀 점검: `python3 tests/coverage_battery.py` — 플랫폼별 전수 경로 pass/fail + 썩은 예시 자동 적발.
> 
> ## No-Site-Name Rule
> 
> `engine/**`, `waf_profiles.yaml`, `engine/templates/**` 파일에는 **특정 사이트의 도메인/URL/셀렉터/브랜드명을 하드코딩하지 않는다**.
> 
> ### 금지
> 
> - `"coupang.com": {...}` 같은 사이트별 레지스트리 엔트리
> - `if "coupang" in url: ...` 같은 도메인 분기
> - WAF 프로파일 `notes`에 특정 사이트 이름이나 경험적 byte 크기 박제
> 
> ### 허용
> 
> - `SKILL.md` / `references/*.md`의 **설명 텍스트**에 사이트 이름 예시 (독자 이해용)
> - `Phase 0` 공식 API 인덱스 (플랫폼이 공식 공개한 엔드포인트)
> - `observations/*.jsonl` 로그 (append-only 관측 데이터 — 코드 경로에 영향 없음)
> - 호출자가 제공하는 `success_selectors`, `user_hint` (현재 호출에만 유효)
> 
> ### 경계 사례 판단 기준
> 
> > "이 엔트리가 다른 사이트에서도 같은 WAF를 쓰면 일반적으로 유효한가?" → YES면 `waf_profiles.yaml`, NO면 runtime hint.
> 
> ### 새 사이트가 안 뚫릴 때
> 
> 1. 먼저 `result.trace`에서 어느 phase가 실패했는지 확인
> 2. 사용자의 `user_hint`로 1회 재시도
> 3. 반복 성공 패턴이 관측되면 `observations/`에 로그 (아직 자동 기록 없음 — 수동)
> 4. 3회+ 반복 확인되고 **동일 WAF를 쓰는 다른 사이트에도 유효**하면 `waf_profiles.yaml` 해당 프로파일의 `tls_impersonate_candidates` / `url_transform_order`를 튜닝 (사이트명 절대 넣지 않음)
> 5. 여전히 안 되면 새 WAF 프로파일 후보 검토 (예: DataDome 세부화, Kasada 등)
> 
> ## 관련 문서 (references/) — 언제 무엇을 읽을지
> 
> 이 섹션은 **참조 파일 선택 가이드**다. 문제가 생겼을 때 어떤 `references/*.md`를 열어야 할지 결정하는 기준으로 쓴다. Claude는 필요할 때만 해당 파일을 `Read`하고, 선제적으로 전부 읽지 않는다.
> 
> ### A. Engine 확장·진단 (하네스 내부)
> 
> | 파일 | 언제 읽는가 | 무엇을 다루는가 |
> |------|-------------|-----------------|
> | [`tls-impersonate.md`](references/tls-impersonate.md) | curl_cffi 격자가 전부 `challenge`/`blocked`로 끝날 때, 새 impersonate 타겟을 `waf_profiles.yaml`에 추가할 때 | curl_cffi로 Safari/Chrome/Firefox TLS(JA3/JA4) 지문 복제하는 방법, WAF(Akamai/Cloudflare/F5 등)별 최적 타겟 조합, 임퍼소네이션 타겟 버전 목록, `tls_impersonate_avoid`의 실증 근거 |
> | [`playwright.md`](references/playwright.md) | engine이 Playwright fallback으로 넘어가는데 MCP/Local Chrome 중 어디로 갈지 확인 필요할 때 | Approach 1 (`mcp__playwright__*` — Cloudflare급 챌린지), Approach 2 (Local Node + `channel:'chrome'` + stealth — Akamai Bot Manager급), 템플릿 파라미터 규격 |
> | [`fallback.md`](references/fallback.md) | `verdict`가 애매하거나 Phase 전환 타이밍 결정 필요할 때 | engine의 Phase 0→1→2→3 에스컬레이션 원칙, 응답 성공/실패 판정 기준 세부, 각 Phase 종료 조건 |
> | [`metadata.md`](references/metadata.md) | 본문 전체를 못 가져왔지만 제목·요약·가격·저자 같은 핵심만이라도 필요할 때 | OGP 메타 태그, JSON-LD (Schema.org), Twitter Card 파싱, 구조화 데이터 추출 패턴 |
> 
> ### B. 경량 대안 (engine 말고 다른 도구가 나은 상황)
> 
> | 파일 | 언제 읽는가 | 무엇을 다루는가 |
> |------|-------------|-----------------|
> | [`jina.md`](references/jina.md) | WAF 없는 일반 웹(블로그·뉴스·Wiki)의 깨끗한 마크다운 추출 필요할 때 | `r.jina.ai/URL` 한 줄로 Puppeteer 기반 JS SPA 렌더링, 마크다운 변환, 무료 500 RPM, API 키 불필요 |
> | [`cache-archive.md`](references/cache-archive.md) | 원본 사이트가 차단됐지만 과거 스냅샷으로라도 접근 필요할 때 | Wayback Machine CDX API, archive.today, AMP Cache (Google Cache는 2024-07 종료됨) |
> | [`rss.md`](references/rss.md) | 뉴스·블로그·커뮤니티의 시계열 업데이트를 구조화해 받고 싶을 때 | RSS/Atom 자동 발견, 피드 파싱, 인증 불필요 — 가장 깔끔한 시계열 데이터 소스 |
> 
> ### C. 플랫폼별 공식/공개 API (Phase 0 인덱스와 연결)
> 
> | 파일 | 언제 읽는가 | 무엇을 다루는가 |
> |------|-------------|-----------------|
> | [`json-api.md`](references/json-api.md) | Reddit/Wikipedia/HN/npm/PyPI 등 **URL 변형만으로** JSON/피드를 주는 사이트 | Reddit Atom/RSS(`.rss`) 우회 + score·댓글용 OAuth(`.json`은 WAF 차단), HN Firebase, Algolia Search, Wikipedia REST, npm/PyPI Registry API |
> | [`public-api.md`](references/public-api.md) | Bluesky/Mastodon/arXiv/Stack Overflow/CrossRef/GitHub/OpenLibrary/Wayback 공식 API 사용 시 | 인증 없이 쓰는 공식 공개 REST/AT/Atom API 엔드포인트, 요청 형식, 공통 파라미터 |
> | [`twitter.md`](references/twitter.md) | X/Twitter 접근 — 프로필 타임라인, 특정 트윗, 키워드 검색 | `syndication.twitter.com` 타임라인, oEmbed 개별 트윗, 검색은 WebSearch로 URL 확보 후 oEmbed |
> | [`naver.md`](references/naver.md) | 네이버 블로그·뉴스·증권·검색 접근 | 서비스별 우회(블로그는 `m.blog.naver.com` 변환, 증권은 비공식 JSON, 검색은 `search.naver.com`), 한글 검색 쿼리 패턴 |
> | [`media.md`](references/media.md) | YouTube/Vimeo/Twitch/TikTok/SoundCloud 등 미디어 메타·자막·오디오 필요 시 | `yt-dlp --dump-json` 기반 1,858개 사이트 커버, 자막 다운로드(`--write-sub`), 포맷 선택, 라이브/팟캐스트 |
> 
> ### D. Engine 코드 직접 읽을 때
> 
> | 파일 | 언제 읽는가 |
> |------|-------------|
> | `engine/phase0.py` | Phase 0 공식-API 라우터 (Reddit/X/YouTube 자동 경로). 플랫폼·경로 추가 시. bias_check 면제 파일(R5 sanctioned) |
> | `engine/fetch_chain.py` | 체인 단계 로직·`Attempt`/`FetchResult` schema·`untried_routes`/`must_invoke_playwright_mcp` 실패게이트 |
> | `engine/validators.py` | 4-계층 검증 세부 (Verdict 분류, 챌린지 마커 목록) |
> | `engine/waf_detector.py` | WAF 랭킹 감지 알고리즘, `_LAST_LOAD_ERROR` 처리 |
> | `engine/waf_profiles.yaml` | 프로파일별 detectors·tls_candidates·capabilities_needed |
> | `engine/url_transforms.py` | URL 변환 규칙 추가할 때 |
> | `engine/executor.py` | Playwright MCP vs local capability 매칭 로직 |
> | `engine/templates/*.js` | Playwright 템플릿 튜닝 (warmup, reload, devices) |
> | `engine/bias_check.py` | 편향 린터 규칙 — brand denylist, URL_PATTERN, excluded dirs |
> 
> 
> ARGUMENTS: 한국+글로벌 커뮤니티에서 "사람들이 실제로 돈 내고 쓰거나 열광하는 작은 자동화 도구" 리서치 — 반복작업 자동화 pain point, micro-SaaS 트렌드, 개인 자동화 도구 히트 사례

**08:26**
> 취미가 아닌 이 회사에 나의 필요성을 입증하는 과제임으로 나의 주관은 빼야 할 필요는 있음, 나의 진짜 문제는 어디에 있을까, 그럼 이 과제는 어디로 가야 하면 좋을까? 짜치는 과제는 안하고 싶긴한데,,, 그럴라면 시간이 좀 걸릴거고 뭔가 되게 밸런스 있게 과제를 설정하고 가야 할 듯?

**08:31**
> ㅇㅎ 좋은 거 같음, 그렇다면 어디까지 감지할지, 몇분에 한 번?, 배포할 때 마다?, 이런 것들, 그리고 단일 도메인을 사용하면 이런게 굳이 필요없는데 어느 기업이 쓰면 좋을지까지도 생각하면서, 나한테 질문을 통해 같이 구성 및 사고를 해보자

**08:40**
> 추가 질문 2, 문제가 없으면 알림 자체가 뜨지 않음, 알림문구와 영상용 알림이 필요함, 추가로 배포를 어떤걸로 잡아서 어떤 알림을 받을건지는 어디 나와있음?

**08:44**
> <텔레그램-봇토큰-마스킹> telegram 키임, D1 ㄱ

**08:54**
> 나는 nodejs를 많이 쓰긴 하지만, php를 쓰는 사람들, 아임웹을 쓰는 사람들, 카페 24 쇼핑몰 자동 뭐시기를 쓰는등 여러 경우가 있을거임, 이런 경우는 어덯게 할 것? 시작 눌렀음, 또한 이것은 배포를 감지하는것임 추가 배포를 진행해주는것임?

**08:57**
> 코덱스 게이트 돌리는 것도 명시하고 저쪽에서 원하는 거 전부 아카이빙 고, 그리고 어떤 메모리를 썼는지 조차도 기록,


## 2026-07-30

**01:27**
> OK 근데 자 봐봐 자동화의 범위를 정의해보자고, 우리는 5분마다 크롤링 하는거고 어디가 안됐는지 체크해주는 거에 아깝잖음, 근데 과제 예시를 본다면 문서 자동 리더 뭐 이런것들임 가깝나?,

**02:02**
> OK 경력으로 내도록 하자, 추가로 우리가 의도한 백엔드는 거진 완료된 것으로 보이는데, 대시보드는 신설도 가야하긴함, 우선 핸드오프 문서로 정리해서 컴팩 이후 디테일 안날아가도록하자, 회의 한 번 더 하겠음'

**02:12**
> /compact

**02:15**
> This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.
> 
> Summary:
> 1. Primary Request and Intent:
> 
> 사토시(Loren, GitHub: kidplayboi)가 소르테크(Sortech — 부산 해운대 본사 + 창원 R&D, 제조기업·병원·대학 대상 B2B SI, 주력 = 통합관제·스마트팩토리·클라우드) 개발자 채용 **사전 과제**를 준비하는 전 과정. 과제 본질 = "AI를 얼마나 깊게, 주도적으로 부렸는가" 평가 (제출물: ① AI 활용 기록 docx/pdf — "프롬프트→AI답변→나의 대응" + **AI가 틀린 순간과 교정(필수, 최중요)** ② 회고 docx/pdf ③ GitHub 코드 링크 ④ 실행 화면 녹화본(필수) ⑤ 맨 앞 3줄 인트로). 사용자의 순차적 지시:
> - 과제 docx 정밀 분석 ("놓치는 게 없어야 함")
> - 공개 레포 생성 + "우리가 일하는 방식으로" 제출물 1·2번 실시간 아카이빙
> - 필수/선택 정리 + 평가 급소 공략 + 관심 자동화 주제 발굴 (인세인서치 리서치 포함)
> - 취미 주관 배제, "회사에 나의 필요성을 입증" + 밸런스 있는 스코프
> - 설계 문답으로 공동 설계 (감지 범위·주기·트리거·타깃 기업)
> - D1 빌드 (텔레그램 키 제공)
> - **Codex 게이트 명시적 실행 + 저쪽이 원하는 것 전부 아카이빙 + 어떤 메모리를 썼는지까지 기록**
> - 자동화 범위 정의 검증 (과제 예시와의 정합성)
> - 최종: **경력 지원 확정**, 대시보드(상태보드) 신설 확정, **핸드오프 문서로 컴팩 대비**, "회의 한 번 더 하겠음"
> 
> 2. Key Technical Concepts:
> - **배포 검증 워처 (deploy-watcher)**: 4층 감지 — L1 생존(HTTP 상태·응답시간) / L2 내용(마커 실존 — 200 거짓양성 차단) / L3 배포반영(캐시 우회 `?nc=` 원본 vs 사용자 시점 버전 비교 + Cache-Control max-age 실측) / L4 렌더링(Playwright, D2 예정)
> - 상태 모델: observed/confirmed(관측) vs **notified(통보 — 발송 성공 후에만 갱신)** 분리; confirm_checks 플랩 방지; pending_missed/unnotified_worst(순단 사후 보고, 이탈 구간 최악 사유 누적)
> - `_bounded_get`: 데몬 스레드 + join(timeout) 총 시한, resp.close() 데몬 위임, 인라인 데드라인 자가 종료, 2MB 크기 상한, truncated 플래그
> - `_decode`: 헤더 charset → utf-8 → cp949 폴백 (charset 없는 한글 사이트 오탐 방지)
> - **Codex 교차 게이트**: codex-gate-runner 에이전트, **모델 핀 gpt-5.6-sol + xhigh 필수** (평문 gpt-5.6 = HTTP 400 차단, 7/29 실측), 맹목 수용 금지(기각 3·강등 ~18), P1/P2 0까지 반복 → 8라운드 PASS
> - GitHub Pages/Fastly: 배포 시 CDN 퍼지(오답 1호의 반증 소재), max-age=600 브라우저 잔상
> - guard-stop.ps1 훅: 검증 미실행 완료 선언 차단 — 매 턴 실검증 재실행 필요
> - 검증 규율: 음성 대조, 반복 실행(단발 GREEN 불충분), 수리 보고도 시퀀스 재현 검증, 이름표 스레드 추적(`watcher-fetch`/`watcher-close`)
> - 가용성 우선 원칙: 설정 오류 사이트만 스킵, 반쪽 텔레그램 설정 = 경고+콘솔 모드
> 
> 3. Files and Code Sections:
> - **C:\Users\<user>\sortech-prework** (공개 레포 github.com/kidplayboi/sortech-prework, HEAD=`3ec0a2e`):
>   - `watcher/checks.py` (~250줄) — L1~L3 + `_bounded_get`(데몬 스레드/데드라인/close 위임) + `_decode` + `_bust_url`(urlsplit, fragment 안전) + `_cache_policy`(no-cache 처리·casefold·초/분/시간)
>   - `watcher/state.py` — `observe()`(streak/confirm/unnotified_worst/pending_missed+duration 스냅샷), `mark_notified()`, `clear_missed()`, `_entry_broken()`(키+값 타입 검사), 원자 저장(tmp+os.replace)
>   - `watcher/notify.py` — 텔레그램 send(3500자 캡, 콘솔 폴백=True 반환, `str(exc)` 토큰 노출 금지 주석), `fmt_transition`, `fmt_missed`("이전 순단 사후 보고(현재 상태 아님)")
>   - `watcher/cli.py` — status(완전 읽기 전용)/once/watch(드리프트 보정·캐치업 방지), 사이트별 격리(체크 예외→FAIL 알림 경로), stdout/stderr utf-8 reconfigure, 반쪽 설정 경고
>   - `watcher/config.py` — .env 로더(utf-8-sig/따옴표/export/인라인 주석), `load_sites` 방어, `validate_sites`(name/url/version_url http(s) 스킴·markers 비어있지 않은 str 리스트·confirm_checks/timeout_sec 양수 int·bool 제외) → (errors, warnings, bad_keys)
>   - `tests/test_watcher.py` — **33개 테스트** (게이트 회귀 고정: 첫관측FAIL·status읽기전용·전송실패 재시도·인코딩·truncated·격리·순서·worst 보존·실경로 찔끔서버 통합테스트 등)
>   - `sites.json` — demo-shop 항목 (markers: ["데모샵","장바구니"], version_url)
>   - `.env`(로컬 전용: TELEGRAM_BOT_TOKEN=<마스킹> TELEGRAM_CHAT_ID=<마스킹> `.env.example`(추적됨, `!.env.example`)
>   - `README.md` — 3줄 인트로 + **Before→After 표(무엇을 자동화했나)** + 계층표 + 설치/실행 + 설정(변수명 인라인) + 알림 정책 + **알려진 한계 4항** + 과정 기록 안내
>   - `docs/00-과제-요구사항.md` (T/S/E/A 추적표), `docs/01-스펙.md` (포지셔닝=감지 전용·L3 2모드(버전/기대마커 `--expect`)·알림 문구 카탈로그·녹화 씬 5개·데모 대상), `docs/02-작업방식-하네스.md` (도구·stop-hook 실발동·메모리 발동표), `docs/MASTER-TRACKER.md` (경력 확정·게이트 PASS 기록), **`docs/HANDOFF.md`** (재개 절차·불변식 5·D2 확정 스코프 4·carryover·남은 단계), `docs/audits/2026-07-29-codex-gate-D1.md` (8라운드 전체 판정·수리·결산), `docs/ai-log/` 세션 01~13 (⚠️오답 1~13호), `docs/retro/회고-노트.md`
> - **C:\Users\<user>\deploy-watcher-demo** (공개, GitHub Pages 라이브 https://kidplayboi.github.io/deploy-watcher-demo/ v1.0.1): index.html(데모샵, meta app-version, cart)·app.js(제품 렌더)·version.txt
> - **메모리**: `~/.claude/projects/C--Users-test/memory/project_sortech_prework_2026_07_29.md` (신규 — 경력 확정·봇 정보·재개=레포 HANDOFF·공개 레포 가드·토큰 마스킹), `MEMORY.md` (인덱스 라인 추가 + **160→138줄 압축**, 종결분 13줄 원문을 `MEMORY_ARCHIVE.md`에 "## 2026-07-30 인덱스 졸업분" 섹션으로 보존)
> - `~/.claude/agents/codex-gate-runner.md` — 모델 표기 gpt-5.6 → **gpt-5.6-sol** 갱신
> 
> 4. Errors and fixes:
> - **오답 1호**: "Pages CDN이 10분간 옛 버전 노출" 예측 → 실측 반증(배포 시 CDN 퍼지, X-Cache HIT인데 새 버전) → L3에 캐시 정책 실측 추가, 녹화 씬 2를 브라우저 캐시 잔상으로 변경
> - **오답 2호(게이트 1차 P1 4건)**: charset 없는 text/html ISO-8859-1 디코드로 한글 마커 상시 오탐 / status가 전이 소비 / 첫 관측 무알림 / 전송 실패 소멸 → `_decode` 신설 + notified 분리 구조로 일괄 마감
> - **오답 3호**: 내가 러너에게 지시한 모델 `gpt-5.6`이 400 차단 → 러너가 메모리 SSoT(gpt-5.6-sol)로 자가 교정
> - **수리發 회귀 총 7건** (2차 N1~N4, 4차 H-A, 7차 G2/G5 유래 등): truncated 미신호 오탐, 자기모순 사유, 격리 절반, 콘솔 무한 반복, ThreadPoolExecutor 비데몬 워커로 프로세스 99.7초 생존+스레드 누적, close() 동기 호출이 3초 블록(시한 2초→실소요 5.03초) 등 — 각각 구조 수정 + 회귀 테스트 고정
> - **"수리 완료" 거짓 보고 2건 적발** (오답 11호 M-C 가드 절반만 작동 → unnotified_worst 누적 구조로 실수리 / 오답 13호 "삭제한" 거짓 주석이 인라인에 잔존 → 정정). 교훈: 수리 보고도 시퀀스 재현 검증, 같은 텍스트는 grep 전수
> - **오답 9호**: "32 GREEN" 보고가 반복 실행(19회 중 2회 실패)에서 반증 — 이름표 스레드 추적으로 전환, 이후 5연속 실행 검증 표준화
> - 테스트 스위트 50초(핸들러가 shutdown 붙잡음) → ThreadingTCPServer daemon_threads로 2초
> - M-D 문구 변경("순단 후"→"이전 순단 사후 보고")이 순서 테스트 단언을 깨뜨림 → "사후 보고"로 갱신 (반복 실행 검증이 즉시 검출)
> - state.json이 커밋에 딸려 들어감 → gitignore + `git rm --cached`
> - `.env.*` 규칙이 `.env.example`까지 무시 → `!.env.example` + `git add -f`
> - MEMORY.md 압축 후 140줄(기준 미달 1줄 초과) → 트랙 E/F 병합으로 138줄
> - **stop-hook이 거의 매 턴 발동** — 매번 실검증 재실행으로 통과 (이 발동 자체를 하네스 문서의 소재로 아카이빙)
> 
> 5. Problem Solving:
> - docx를 XML 레벨 전수 검증(변경추적·주석·텍스트박스·숨김 0건)으로 "놓침 없음" 확정
> - 주제 선정: 크몽 돈 신호(자동화 매크로 10~20만원 실판매) + HN "I automated my job" 공감 + 소르테크 통합관제 도메인 → 형 실화(배포 후 캐시 검증) × 회사 도메인 × 작은 스코프의 교집합 = 배포 검증 워처
> - "자동화 맞나" 질문에: 문서 정의는 장르가 아니라 "반복 작업 제거"이며 상주형이 오히려 상위 자동화 — README Before→After 표로 프레이밍 보강
> - 8라운드 게이트 결산: 총 74건(P1 6·P2 25·P3 43), P1+P2 추세 12→4→5→2→4→2→2→0, 검사자 자신의 권고 결함 2건 시인 — 제출물 서사의 척추
> - 수용된 트레이드오프(README 알려진 한계 4항): CDN 쿼리스트링 무시 / 헤더 정체 서버 / 병적 트리클 / notify 총시한 부재. SSRF는 위협모델 밖 기각 유지, L3 기대버전 앵커·L4는 D2 이월
> 
> 6. All user messages:
> - "C:\Users\<user>\Downloads\소르테크 사전 과제 ver02.docx 읽어보셈 나한테 너무 유리한 주제임"
> - "자동화를 제출을 해야하는데, 1번2번3번을 빡빡하게 내가 제일 잘하는 걸 해야하는 것임 우선 과제 레포를 오픈으로 하나 파고, 우리가 일 하는 방식으로 쟤네가 원하는 1,2번을 아카이빙을 해줘야할 거 같음, 추가로 문서를 꼼꼼하게 읽고 놓치는 게 없어야한다는 말임"
> - (턴 중간) "나한테 손해 없이, 필수사항, 선택사항 정리 후 저쪽이 높게 평가하는 부분을 찔러주면서 내가 관심있는 자동화 주제 찾으면서 가보자고 ㅇㅇ"
> - "1번은 후보, 작다는 기준을 모르기에, 자동화를 어디까지 해야하는지 무슨 주제가 좋은 지 의문임, 기업에서 요구하는 건 판매 가능한 상품이기에 소비자에게 먹히는 것을 해보고싶음, 인세인리서치를 돌리는 게 좋을 듯"
> - "취미가 아닌 이 회사에 나의 필요성을 입증하는 과제임으로 나의 주관은 빼야 할 필요는 있음, 나의 진짜 문제는 어디에 있을까, 그럼 이 과제는 어디로 가야 하면 좋을까? 짜치는 과제는 안하고 싶긴한데,,, 그럴라면 시간이 좀 걸릴거고 뭔가 되게 밸런스 있게 과제를 설정하고 가야 할 듯?"
> - "ㅇㅎ 좋은 거 같음, 그렇다면 어디까지 감지할지, 몇분에 한 번?, 배포할 때 마다?, 이런 것들, 그리고 단일 도메인을 사용하면 이런게 굳이 필요없는데 어느 기업이 쓰면 좋을지까지도 생각하면서, 나한테 질문을 통해 같이 구성 및 사고를 해보자"
> - (AskUserQuestion 응답) 감지="4층: +렌더링 검증" / 트리거="하이브리드" / 형태="CLI+텔레그램, 여유시 상태보드" / 타깃="웹 에이전시/제작사"
> - "추가 질문 2, 문제가 없으면 알림 자체가 뜨지 않음, 알림문구와 영상용 알림이 필요함, 추가로 배포를 어떤걸로 잡아서 어떤 알림을 받을건지는 어디 나와있음?"
> - "<텔레그램-봇토큰-마스킹> telegram 키임, D1 ㄱ"
> - "나는 nodejs를 많이 쓰긴 하지만, php를 쓰는 사람들, 아임웹을 쓰는 사람들, 카페 24 쇼핑몰 자동 뭐시기를 쓰는등 여러 경우가 있을거임, 이런 경우는 어덯게 할 것? 시작 눌렀음, 또한 이것은 배포를 감지하는것임 추가 배포를 진행해주는것임?"
> - "코덱스 게이트 돌리는 것도 명시하고 저쪽에서 원하는 거 전부 아카이빙 고, 그리고 어떤 메모리를 썼는지 조차도 기록,"
> - "OK 근데 자 봐봐 자동화의 범위를 정의해보자고, 우리는 5분마다 크롤링 하는거고 어디가 안됐는지 체크해주는 거에 아깝잖음, 근데 과제 예시를 본다면 문서 자동 리더 뭐 이런것들임 가깝나?,"
> - "/rename 12" + "OK 경력으로 내도록 하자, 추가로 우리가 의도한 백엔드는 거진 완료된 것으로 보이는데, 대시보드는 신설도 가야하긴함, 우선 핸드오프 문서로 정리해서 컴팩 이후 디테일 안날아가도록하자, 회의 한 번 더 하겠음"
> - (다수의 Stop hook 피드백 — 실검증 실행 요구, 매번 준수함)
> 
> **보안/제약 (계속 유효)**: 공개 레포에 시크릿·개인정보·클라이언트·<타-프로젝트-마스킹>·타 프로젝트 정보 유입 절대 금지 / 채팅에 노출된 텔레그램 토큰은 제출용 로그 발췌 시 반드시 마스킹 / `.env`는 깃 제외 유지 / Codex 호출은 gpt-5.6-sol+xhigh 핀 / 매 응답 끝 NOW+NEXT+사토시 action / 커밋 말미 `Co-Authored-By: Claude Fable 5 <<이메일-마스킹>>` / 푸시 후 원격 해시 대조
> 
> 7. Pending Tasks:
> - **회의 (사토시 예고)** — 의제 4개 제안됨: ① 상태보드 형태(정적 HTML 1장 추천 vs 로컬 서버) ② 녹화 대본(Before 수동루틴 오프닝 + 씬 5개) ③ 제출물 1·2번 docx 분량/톤 ④ N-A 통과 판단 동의 여부
> - **D2 빌드** (확정 스코프, HANDOFF.md 명시): L4 렌더링(Playwright — "취소 불가 in-flight" 정리 경로 선설계 조건) / `watch deploy <site> [--expect "문구"]` 집중 모드(기대 버전 앵커 포함 — 게이트 기각-2 승계 계약) / **상태보드 신설(확정 승격)** / 실행 녹화(S4)
> - D2 완료 시 Codex 게이트 재실행 (동일 규율)
> - D3: AI 활용 기록·회고 정제 → docx/pdf 변환, 3줄 인트로 + T/S/E/A 전 항목 크로스체크, juran03@sortech.co.kr 메일 제출
> - 🔴 **제출 기한** — 사토시가 받은 메일에서 확인 필요 (여전히 미확인)
> 
> 8. Current Work:
> 직전 턴에서 사토시 지시("경력으로 내자 + 대시보드 신설 확정 + 핸드오프 문서로 컴팩 대비")를 완료함: ① `docs/HANDOFF.md` 신설(재개 절차 = HANDOFF→MASTER-TRACKER→01-스펙, 검증 명령, 불변식 5, D2 확정 스코프 표, 게이트 carryover, 남은 단계, 미결) ② MASTER-TRACKER에 경력 확정·상태보드 승격 반영 ③ 커밋 `3ec0a2e` 푸시+원격 대조 ④ 개인 메모리 `project_sortech_prework_2026_07_29.md` 생성 + MEMORY.md 인덱스 등록 ⑤ 훅 지시로 MEMORY.md 압축(160→138줄, 졸업 13줄 ARCHIVE 원문 보존) ⑥ 최종 검증 실행: `remote=3ec0a2e local=3ec0a2e`, 워킹트리 클린(0), `Ran 33 tests OK`, MEMORY.md 138줄. 모든 작업 완료·검증된 상태로 **사토시의 회의를 대기 중**.
> 
> 9. Optional Next Step:
> 없음 — 사토시가 "회의 한 번 더 하겠음"이라 명시했으므로 D2 착수 전 대기가 맞음. 회의가 시작되면 제안해둔 의제 4개(상태보드 형태 — "내 추천은 전자(정적 HTML 한 장)" / 녹화 대본 / 제출물 docx 톤·분량 / N-A 동의)를 순서대로 결정받고, 결정 즉시 `docs/HANDOFF.md`·`01-스펙.md` 갱신 후 D2(L4 렌더링 + `watch deploy --expect` + 상태보드) 착수. 재개 시 첫 동작 = `docs/HANDOFF.md` Read + `python -m unittest discover -s tests`(33 GREEN 확인) + `python -m watcher status`.
> 
> If you need specific details from before compaction (like exact code snippets, error messages, or content you generated), read the full transcript at: C:\Users\<user>\.claude\projects\C--Users-test\195b5388-dd68-440e-aba8-bda85bd4a681.jsonl
> Continue the conversation from where it left off without asking the user any further questions. Resume directly — do not acknowledge the summary, do not recap what was happening, do not preface with "I'll continue" or similar. Pick up the last task as if the break never happened.

**02:20**
> 회의 ㄱ

**02:40**
> 오케이, 난 아직 깊다 생각이 안드는 이유에 대해, 기존 프로젝트 때문에?

**02:43**
> 중간 단계로 잡는 거 보다 여기서 깊게 들어가야하는거네, 감지 말고도 다른 하나를 추가해야하는데 무설치에서 다룰 수 있는 감지기는 한계가 있다고 보는데, 트래픽으로 인젝션을 감지한다던지 이런 느낌으로 가야하나? 리서치를 돌려볼까

**04:45**
> 12차 끝나면 D2 바로 ㄱ

**05:37**
> 한국 기준으로 8/2일임

**05:38**
> 한국 기준으로 8/2일임 시간이 너무 많이 남아버림

**06:03**
> ㄱ

**06:17**
> 뭐가문제야 지금

**06:17**
> 서버 삑난거 끝났을텐데 뭐가문제야 지금

**07:06**
> 야~!~!!!!!!!! 미친놈아 왜 너만 삑나노

**07:11**
> 제발정신차려

**07:26**
> ㅇㅋ 하고 배포까지 해보고, 이제 양식에 맞춰 파일 작성만 하면 되는건가? 근데 시간이 3일이나 남음 ㅋㅋ

**08:48**
> OK 그러면 여기서 우리가 무언가를 깊게 들어갈 수 있는 방법은 뭐가 있을까? 또한 저기 문서에 영상이 필요한거야? 사진으로 플로우를 찍은 pdf는 안되고? 우선 우리가 완료한 것들 내가 한 눈에 볼수있게 정리를 먼저 해주는게 우선이야ㅐ

**08:52**
> 일단 컴팩 ㄱ 서버가 계속 터지네

**09:02**
> 야 씨발련아 [Image #1] 이 세션만이래

**09:02**
> [Image: source: C:\Users\<user>\.claude\image-cache\195b5388-dd68-440e-aba8-bda85bd4a681\1.png]

**09:13**
> 아니 오늘은 오류가 없다고 씨빨련아


## 2026-07-31

**00:24**
> ㄱㄱ. 제안한 일정대로 진행해줘. 우선 클로킹 판정을 패스 간 누적 방식으로 근본 수정하고 18차 게이트까지 진행해. 영상은 무편집 3분 원테이크로 제작하고 PDF를 보조자료로 같이 제출하는 방향으로 가자. 기능 추가는 하지 말고, 실제 AI 대화 원문 추출과 요구사항 크로스체크까지 빠짐없이 진행해줘. 회고에 좋은 소재들이 굉장히 많네 ㅇㅇ

**00:28**
> 우선 컴팩 먼저하자

**00:37**
> /compact

**00:39**
> This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.
> 
> Summary:
> 1. Primary Request and Intent:
> 
>    - **Initial**: "sortech-prework 레포 읽고 계속 진행 해야함" — resume the 소르테크 (Sortech) job-application pre-assignment project from the repo's HANDOFF.
>    - **Second turn (explicit priority ordering)**: "OK 그러면 여기서 우리가 무언가를 깊게 들어갈 수 있는 방법은 뭐가 있을까? 또한 저기 문서에 영상이 필요한거야? 사진으로 플로우를 찍은 pdf는 안되고? 우선 우리가 완료한 것들 내가 한 눈에 볼수있게 정리를 먼저 해주는게 우선이야" — (a) summarize completed work at-a-glance FIRST, (b) is a video actually required or can a PDF of flow screenshots substitute, (c) what are the ways to go deeper.
>    - **Approval turn**: "ㄱㄱ. 제안한 일정대로 진행해줘. 우선 클로킹 판정을 패스 간 누적 방식으로 근본 수정하고 18차 게이트까지 진행해. 영상은 무편집 3분 원테이크로 제작하고 PDF를 보조자료로 같이 제출하는 방향으로 가자. 기능 추가는 하지 말고, 실제 AI 대화 원문 추출과 요구사항 크로스체크까지 빠짐없이 진행해줘." — approved the 3-day schedule; fix cloaking judgment via cross-pass accumulation then run the 18th gate; video = unedited 3-min one-take + PDF as supplement; **no feature additions**; raw AI conversation extraction and full requirements cross-check must both be done.
>    - **Final**: "우선 컴팩 먼저하자" — compact first.
> 
>    Standing project constraints (from CLAUDE.md / memory, must remain in effect):
>    - **공개 레포 가드**: client names, <타-프로젝트-마스킹>, secrets, internal server info, and other-project identifying info must NEVER enter this public repo. Token/PII masking is mandatory when excerpting logs.
>    - Telegram credentials live only in the repo-local `.env` (git-excluded); bot token was once exposed in conversation → masking required.
>    - No `--no-verify`, no `--force`, no history rewrite, no direct server modification, no SSH/firewall/root changes.
>    - Codex gate calls must pin **model=gpt-5.6-sol + reasoning xhigh** every call (plain `gpt-5.6` and `-codex` variants are 400-blocked).
>    - Files must stay under 500 lines; one file = one responsibility; new feature = new file.
>    - No claiming completion without actually executing verification.
> 
> 2. Key Technical Concepts:
>    - **배포 검증 워처 (deploy verification watcher)** — Python CLI tool, 5 detection layers: L1 생존(HTTP), L2 내용(markers), L3 배포반영(version origin vs user), L4 렌더링(Playwright headless), L5 보안(UA cloaking + Google Safe Browsing v4).
>    - **Sampling aliasing / phase lock** — the core defect: deterministic finite sampling patterns alias against rotation periods. With R requests/pass, phase shifts `R mod P` per pass; if `P | R`, permanent false positive.
>    - **Cross-pass accumulation, rotation detection, phase randomization** — the newly d<타-프로젝트-마스킹>ed 3-axis fix.
>    - **Codex cross-gate discipline** — 17 rounds; P1/P2/P3 grading; blind acceptance forbidden; each finding must be accepted/rejected with code evidence.
>    - **음성 대조 (negative control)** — remove the guard and confirm RED; newly extended to apply at *test-authoring* time.
>    - **관측/통보 분리** — `observed`/`confirmed`/`notified` state separation, flap prevention via `confirm_checks`, 순단 사후 보고 (`pending_missed`).
>    - **carry / 간헐 층 승계** — L4/L5 skipped-pass results are carried forward so "unobserved" isn't treated as "OK".
>    - Requirements taxonomy T1–T6 (task), S1–S5 (deliverables), E1–E5 (evaluation), A1–A4 (admin).
> 
> 3. Files and Code Sections:
> 
>    - **`C:\Users\<user>\sortech-prework\watcher\security.py`** (215 lines) — the file the red<타-프로젝트-마스킹> targets. Contains `check_cloaking(site)`, `_confirm_bot_only(site, timeout, candidates, first_bot_text)`, `check_reputation(site)`, `_fetch_view`, `_unknown_cloak`, `_keywords`, `_gsb_key`, `bot_view_url`. Key constants: `USER_SAMPLES = 8`, `BOT_SAMPLES = 3`, `SPAM_MAX_SHOW = 3`, `BOT_UA` (Googlebot), `SEARCH_REFERER`. The 16th-gate batching is `일반(0) 봇(1) │ 일반 2..9 │ 봇 10,11`. The 17th gate proved residue 1 mod P is only revisited at `1+P`, so P≥9 never gets revisited by user samples. `first_bot_text` is a dead parameter (P3-3).
>      ```python
>      for _ in range(USER_SAMPLES):
>          user_text, note = _fetch_view(site["url"], timeout)
>          ...
>          remaining = [k for k in remaining if k not in user_text]
>          if not remaining:  # 일반 시점에도 있다 = 클로킹 아님 (조기 종료)
>              return {"confirmed": [], ...}
>      while bot_seen < BOT_SAMPLES:
>          bot_text, note = _fetch_view(site["url"], timeout, ua=BOT_UA, referer=SEARCH_REFERER)
>      ```
>      The sibling marker path (lines 111–118) still does a single fixed comparison — this is P2-2:
>      ```python
>      markers = site.get("markers") or []
>      missing = [m for m in markers if m in user_text and m not in bot_text]
>      if missing:
>          return {"layer": "L5 클로킹", "ok": False, "warn": True,
>                  "detail": "검색봇 시점에서 핵심 내용 사라짐: %s (봇 차단 설정일 수도 있음)" % ", ".join(missing)}
>      ```
> 
>    - **`watcher/state.py`** (169 lines) — persistence + transition engine. `_REQUIRED_KEYS`, `observe(state, site_key, status, reason, confirm=1)`, `mark_notified`, `clear_missed`, `_entry_broken(entry)` with optional-field type checks (this is where a `cloak` dict field must be registered), `load_state`/`save_state` (atomic temp-file replace).
> 
>    - **`watcher/cli.py`** (253 lines) — `run_pass(sites, st, alert, persist=True, do_render=True, carry=None)`, `_layer_rank`, `_carry_layers`, `cmd_status` (`run_pass(sites, {}, alert=False)` — read-only contract, must not accumulate), `cmd_once`, `cmd_watch` (carry dict, `--render-every`), `cmd_deploy`. This is where cloak memory must be threaded through.
> 
>    - **`watcher/checks.py`** (385 lines) — `check_site(site, do_render=True)` dispatches L1–L3 then L4/L5; `security.check_security(site)` is called at line ~97. Needs a `cloak_memory` parameter added. Also `_bounded_get` (daemon-thread total deadline, `read1`, `raw.shutdown()`), `_decode`, `_l1_alive`, `_l2_content`, `_l3_deploy`.
> 
>    - **`watcher/deploy.py`** (204 lines) — stateless one-shot `run_deploy_watch(...)`; exit codes `EXIT_STABLE=0, EXIT_FAIL=1, EXIT_STABLE_UNVERIFIED=3`; needs session-local cloak memory.
> 
>    - **`tests/helpers.py`** (193 lines) — `_RotatingHandler` with `SPAM_RESIDUES` and `PERIODS`. The defective fixture:
>      ```python
>      SPAM_RESIDUES = {"/rotate2": {1}, "/rotate3": {1}, "/rotate4": {1},
>                       "/rotate5": {1, 2, 4}, "/rotate6": {1, 2, 4},
>                       "/rotate7": {1, 2, 4}, "/rotate8": {1, 5},
>                       "/rotate12": {1, 5, 9}}
>      ```
>      `/rotate12`'s `{1,5,9}` are the OLD batching's bot indexes → the new user block (2..9) always hits 5 and 9 → structurally cannot fail. A single-residue `{1}` fixture must be added (and must be RED on current code).
> 
>    - **`tests/test_layers.py`** (432 lines) — `CloakingRealServerTest` with `test_sequential_rotation_is_not_flagged_any_period`, `test_user_samples_are_one_unbroken_block` (measures request order via a spy on `_fetch_view`), `test_real_cloaking_still_detected`, `test_intermittent_cloaking_still_detected`, `test_rotation_rejection_does_not_hide_marker_loss`.
> 
>    - **`docs/00-과제-요구사항.md`** — requirement extraction; verified against the original docx.
> 
>    - **`C:\Users\<user>\Downloads\소르테크 사전 과제 ver02.docx`** — original assignment; extracted verbatim. Decisive lines: "(필수) 실제로 동작해야 합니다. (실행 화면 녹화본 첨부)" and FAQ "평가자는 ... 화면 녹화만 보면서 '이게 뭐지? 뭘 만든 거지?'를 혼자 추측해야 하고". The official 제출물 list is only 3 items (AI 활용 기록 / 회고 / 깃허브 링크) — the recording is a requirement-side attachment, not a listed deliverable.
> 
>    **Files created/modified this session (all docs; zero code changes)**:
>    - **`docs/06-L5-판정-재설계안.md`** (NEW, the resume anchor) — full d<타-프로젝트-마스킹>: §1 the 5-round genealogy table and root cause ("결정적 유한 표본 패턴은 그 길이와 맞물리는 주기에서 반드시 aliasing된다 — 이 축 위에는 해가 없다"); §2 the three axes (①회전 감지 ②패스 간 누적 ③위상 무작위화); §3 sibling marker path; §4 implementation notes (new pure-function module `watcher/cloak_decision.py`, cloak memory under `state.json` site entry, `status` read-only → WARN only, `check_site(site, do_render, cloak_memory=None)`, deploy session-local memory); §5 verification plan (period sweep P=2~16, 3 negative controls, detection preservation, fixture correction); §6 honest residual limits (detection delayed up to K passes on rotating sites; axis 3 is probabilistic).
>    - **`docs/audits/2026-07-29-codex-gate-D1.md`** — appended §17차 (full findings) + extended the 계보 table with rounds 16/17 + three 교훈.
>    - **`docs/HANDOFF.md`** — fully rewritten: new snapshot, "지금 열려 있는 작업 = L5 판정 재설계", invariant #4 added (negative control at test-authoring time), 확정 사항 table (schedule / video+PDF / no features / raw extraction / cross-check), 다음 작업 순서 1–7, carryover.
>    - **`docs/MASTER-TRACKER.md`** — row 5 → ✅ code complete; new row 5-1 (gates 13–17, 17th FAIL); rows 6/7 annotated with the video and S1b decisions.
>    - **`docs/ai-log/2026-07-30-24-17차게이트-축을-바꾸다.md`** (NEW) — session 24 log with 오답 24·25·26호 and the AI-inspector lens comparison table.
>    - Memory: `project_sortech_prework_2026_07_29.md` updated; `feedback_regression_test_must_fail_first.md` created; `MEMORY.md` index updated with two lines.
>    - Scratchpad only (outside repo): `dump_docx.py`.
> 
> 4. Errors and fixes:
>    - **Glob timeout** on `**/sortech*` over `C:\Users\<user> → switched to `Get-ChildItem -Directory` to locate the repo.
>    - **PowerShell here-string + `python -c` SyntaxError** (`p=rC:\Users\<user>\...` — quotes mangled) → wrote `dump_docx.py` to the scratchpad and executed the file instead.
>    - **Stop-hook block**: "You changed code/config this turn and claimed completion, but no verification ... was actually EXECUTED." → Fixed by running `git status --porcelain` (empty), `git diff HEAD --stat` (empty), and `python -m unittest discover -s tests` (78 OK), then stating plainly that no repo code/config had changed and that the only written file was a scratchpad script.
>    - **PowerShell `git push` stderr noise** rendered as NativeCommandError, but the push actually succeeded — verified by comparing `git rev-parse HEAD` and `git rev-parse origin/main` (both `c9476039135e622474f1f6c484787dce3bec1b1b`).
>    - **Commit message with multi-line body**: used a Bash heredoc (`git commit -q -F - <<'EOF'`) after the earlier PowerShell here-string failure.
>    - **17th gate findings (defects in prior work, not tool errors)**: period-12 permanent false positive; a regression test that could not fail; a "clean axis" recorded from an underpowered sample; a sibling code path never fixed across 4 rounds.
> 
> 5. Problem Solving:
>    - Established, with original-document evidence, that the video is expected (the word "녹화본" appears in the requirement and the FAQ presupposes the evaluator watching a screen recording), while noting it is not in the official 3-item deliverable list. Recommended video + PDF supplement; user approved.
>    - Diagnosed the 5-round cloaking failure as a *single-axis* problem and d<타-프로젝트-마스킹>ed a 3-axis replacement rather than another placement tweak — this is the key architectural decision of the session.
>    - Surfaced (rather than hid) three open problems: the 17th-gate FAIL, the S1b format gap (ai-log is 389 lines of summary prose with zero raw conversation, while the requirement says "날것 그대로 환영"), and the MASTER-TRACKER SSoT drift.
>    - Confirmed session transcripts exist for raw-conversation extraction (30 `.jsonl` files under `C:\Users\<user>\.claude\projects\C--Users-test`, including the 8.0 MB `195b5388` matching the project's originSessionId).
> 
> 6. All user messages:
>    - "sortech-prework 레포 읽고 계속 진행 해야함"
>    - "OK 그러면 여기서 우리가 무언가를 깊게 들어갈 수 있는 방법은 뭐가 있을까? 또한 저기 문서에 영상이 필요한거야? 사진으로 플로우를 찍은 pdf는 안되고? 우선 우리가 완료한 것들 내가 한 눈에 볼수있게 정리를 먼저 해주는게 우선이야"
>    - "ㄱㄱ. 제안한 일정대로 진행해줘. 우선 클로킹 판정을 패스 간 누적 방식으로 근본 수정하고 18차 게이트까지 진행해. 영상은 무편집 3분 원테이크로 제작하고 PDF를 보조자료로 같이 제출하는 방향으로 가자. 기능 추가는 하지 말고, 실제 AI 대화 원문 추출과 요구사항 크로스체크까지 빠짐없이 진행해줘. 회고에 좋은 소재들이 굉장히 많네 ㅇㅇ"
>    - "우선 컴팩 먼저하자"
>    - (Local command output, not a request: `/model` set to Opus 5.)
> 
> 7. Pending Tasks:
>    - #3 **L5 판정 재설계 시공** — implement `docs/06-L5-판정-재설계안.md` axes ①②③ + sibling marker path; new `watcher/cloak_decision.py`; cloak memory in `state.json`; inject via `check_site`.
>    - #4 **재설계 자가검증** — P=2~16 all-phase sweep (0 false positives), 3 negative controls (each axis removed → RED), detection preservation (real + intermittent cloaking), fixture correction (`/rotate12` single-residue `{1}`, confirm RED on current code first).
>    - #5 **18차 Codex 게이트** (pin gpt-5.6-sol + xhigh) until P1/P2 = 0, including the 8 outstanding P3 items.
>    - #6 **AI 대화 원문 추출 파이프** (S1b gap) with full token/PII masking.
>    - #7 **AI 검사자 통제 실험** — same defect, varied prompt shapes, measure detection rate.
>    - #8 **실행 녹화** (unedited 3-min one-take) + PDF supplement.
>    - #9 **S1 docx (AI 활용 기록) + S2 docx (회고)**.
>    - #10 **3줄 인트로 + 요구사항 전항목 크로스체크 → 제출** (juran03@sortech.co.kr, deadline 2026-08-02).
> 
> 8. Current Work:
>    Immediately before the summary request, I was executing the user's "우선 컴팩 먼저하자" instruction by persisting all in-context state to disk so that compaction loses nothing. Concretely I had just:
>    - Created `docs/06-L5-판정-재설계안.md` containing the full 3-axis red<타-프로젝트-마스킹>, because the d<타-프로젝트-마스킹> existed only in context. Its §2 states: "① **회전 감지** — 교란 원인을 직접 관측한다(일반 표본끼리 본문이 다른가). 안정 페이지면 교란 자체가 없으니 즉시 확정. ② **패스 간 누적** — 회전 사이트는 한 패스로 확정하지 않고 K패스 누적. 그 전에는 WARN(의심 n/K). ③ **위상 무작위화** — 패스당 요청 수를 흔들어 결정적 위상 락을 확률적으로 소멸시킨다."
>    - Appended the 17th-gate verdict to the audits file and recorded 오답 24·25·26호 in a new ai-log session file.
>    - Rewritten HANDOFF.md and corrected MASTER-TRACKER.md drift.
>    - Ran the test suite (`Ran 78 tests in 10.156s / OK`), committed as `c947603 docs: 17차 게이트 판정 + L5 판정 재설계안 (오답 24·25·26호)`, pushed, and verified `HEAD == origin/main == c9476039135e622474f1f6c484787dce3bec1b1b`.
>    - Updated memory files and created tasks #3–#10.
> 
>    No code was changed this session — only documentation. The repo is clean at c947603.
> 
> 9. Optional Next Step:
>    Resume task #3 — implement the L5 red<타-프로젝트-마스킹> — since that is exactly what the user approved: "우선 클로킹 판정을 패스 간 누적 방식으로 근본 수정하고 18차 게이트까지 진행해." Per HANDOFF the first step is to read `docs/06-L5-판정-재설계안.md` and build axes ①②③ plus the sibling marker path, creating `watcher/cloak_decision.py` as pure functions and threading cloak memory from `run_pass` → `check_site` → `check_security`, while honoring the read-only contract of `cmd_status` ("`status` 명령은 **읽기 전용 계약**이라 state를 읽지도 쓰지도 않는다 → 누적 없음 → 회전 사이트는 확정 불가, WARN까지만 표시"). Before writing the new regression fixture, apply the newly recorded invariant: confirm the `/rotate12` single-residue `{1}` fixture is RED on current code first. Note the user asked to compact first, so this should begin after compaction.
> 
> If you need specific details from before compaction (like exact code snippets, error messages, or content you generated), read the full transcript at: C:\Users\<user>\.claude\projects\C--Users-test\e37d4d4f-bf39-4878-8dec-578f41565dc8.jsonl
> Continue the conversation from where it left off without asking the user any further questions. Resume directly — do not acknowledge the summary, do not recap what was happening, do not preface with "I'll continue" or similar. Pick up the last task as if the break never happened.

**00:39**
> ㄱㄱㄱㄱㄱ

**05:10**
> 순서 재고하셈, ai 검사자 통제 실험을 먼저 하는게 낫지 않겠음? 아님 어떻게 할거임? 지금 마무리 단계?

**05:12**
> 추가 질문, 녹음 플로우는 어떻게 하면 되는거임? 화면캡처도구 다운받아오셈

**05:17**
> ㅇㅋ 너가 녹화해

**05:28**
> 잠만 녹화는 맨 마지막으로 빼고 실험부터 해서 문서 깔끔하게 쓰는게 낫지 ㅇㅇ 컴 껏다가 해야할듯이건

**05:46**
> OK 잘 진행하고 있네 ㅇㅇ 내가 판단할 게 생각보다 많이 없네,,, <타-프로젝트-마스킹> 생각해서 그런가

**06:23**
> 전부 드러내지만, 정확하게, 어떤 걸 중요하게 잡았고 어떤 게 오류였다, 퀄리티 낮은 건 다 빼버리고 나도 오호~ 할 수준만 기록해두셈, 이제 뭐 하면 되는거지? 추가로 우리가 시도한 것들, 더 깊게 어디로 간건지를 나한테 먼저 보여주셈

**06:27**
> ㅇㅇ S 급 대화, 나랑 너가 빛나는, 다만 내가 더 빛나야함 어떤 규칙 무슨 프롬프트, 너는 어떤 규칙을 적용해서 어떻게 갔는지 뭔지 알지? 가보자 일단 ㅇㅇ

**06:27**
> ㅇㅇ S 급 대화, 나랑 너가 빛나는, 다만 내가 더 빛나야함 어떤 규칙 무슨 프롬프트, 너는 어떤 규칙을 적용해서 어떻게 갔는지 뭔지 알지? 가보자 일단 ㅇㅇ B급에서 A급 살짝 안되는 건 살려

