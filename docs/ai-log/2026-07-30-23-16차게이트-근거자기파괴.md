# 2026-07-30 세션 23 — 16차 게이트: 내가 세운 근거를 내 코드가 깨고 있었다

**도구**: Claude Code (Claude Fable 5) + Codex (gpt-5.6-sol xhigh, 러너 에이전트 경유)

## 판정: FAIL (P1 0 · P2 1 · P3 3) — 전문 = audits §16차

15차 P3 5건은 전부 코드 실측으로 이행 확인(**거짓 수리 0건** — 15차의 오답 22호와 대비되는 개선). 남은 1건이 클로킹 오탐 **4라운드째**.

## ⚠️ 오답 23호 — 근거를 코드가 스스로 깨뜨렸다

15차에 "일반 시점을 **연속** 8회 뜨면 회전 주기 8까지 모든 회차를 덮는다"는 수학적 근거로 재설계했다. 그런데 같은 함수 안에서 봇 표본을 `index % 3 == 2` 자리에 끼워 넣었다 — **그 봇 요청이 인덱스를 소비해 일반 표본이 더 이상 연속이 아니게 된다**. 계측된 실제 배치는 일반 `{0,2,3,4,6,7,8,10,11}`·봇 `{1,5,9}`. 결과: 주기 4·8·12에서 6/6 오탐(4·12는 영구), 그리고 **하필 4와 8은 내가 "구조적으로 안전"이라 문서에 적은 범위 안쪽**이었다. 주기 4(배너 4개 로테이션)는 현실에서 가장 흔한 형태다.

두 겹의 교훈:
1. **근거와 구현이 같은 함수 안에서 어긋날 수 있다.** 나는 "연속 8표본"이라 쓰면서 그 연속을 깨는 코드를 같은 루프에 넣었다. 근거를 문장으로 적는 것과 코드가 그 성질을 갖는 것은 별개다.
2. **그래서 성질 자체를 테스트로 단언해야 한다.** 이번엔 요청 순서를 계측해 "일반 표본 인덱스가 연속이다"를 직접 단언하는 테스트를 넣었다(`test_user_samples_are_one_unbroken_block`). 누가 다시 봇을 블록 중간에 넣으면 즉시 RED다. 회귀 테스트가 결과만 보던 것에서 **근거의 전제**를 보게 바꾼 것 — 같은 결함을 4번 반복한 뒤 얻은 장치.

회귀 테스트 커버리지도 문제였다: 주기 2·3·5·6·7만 있어서 4·8·12가 통과했다. 취약 구간은 "예시로 준 주기"가 아니라 **표본 배치에서 수학적으로 유도**해야 했다.

## 수리 내역

일반 8표본을 끊기지 않는 블록으로(봇은 블록 밖 뒤로) · 회귀 테스트에 주기 4·8·12 + 취약 잔여류 추가 · **연속성 계측 단언 테스트 신설** · `_layer_rank` 두 자리 층 이름 파싱 · 최악 요청 수 문서 정정(11→12 실측) · 안전 범위 서술을 실제 인덱스 배치로 재작성.

## 검증

78개 5연속 GREEN · **음성 대조**(봇을 블록 중간으로 되돌림 → 주기4 오탐 + 연속성 단언, 정확히 RED 2건) · 라이브 5층 🟢.

## 적용 메모리/규칙

증상 vs 클래스(취약 조건을 수학으로 유도해 테스트에 반영) · **근거의 전제를 테스트로 단언** · 음성 대조 필수 · 서술보다 동작 먼저 확인
