# 2026-07-30 세션 21 — 13·14차 게이트 (D2 신규 4슬라이스 검증) → 수리

**도구**: Claude Code (Claude Fable 5) + Codex (gpt-5.6-sol xhigh, 러너 에이전트 경유)

**나**: "ㅇㅋ 하고 배포까지 해보고, 이제 양식에 맞춰 파일 작성만 하면 되는건가?"

## 판정: 13차 FAIL(P1 1·P2 7) → 수리 → 14차 FAIL(P1 0·P2 3) → 수리

전문 = audits 문서 §13차·§14차. D2는 신규 기능 4개라 지적 총량이 컸고(P1+P2 = 8 → 3), 13차 P1은 14차에서 완전 종결됐다.

## ⚠️ 오답 19호 — 수리가 "검사자가 준 예시"에만 맞춰졌다

14차 판정문의 진단이 정확했다. 13차에서 검사자는 클로킹 오탐을 **랜덤 회전 서버**로 재현해 보여줬고, 나는 "1회 재표본해서 재현되면 확정"으로 고쳤다. 그런데 재표본을 1차와 **같은 순서**(일반→봇)로 떴기 때문에, **순차 회전** 사이트에서는 요청 순서와 회전 위상이 계속 맞물려 오탐이 8/8 그대로 재현됐다. 더 나쁜 건 문구였다 — "2회 연속 재현"이 붙어 운영자가 "이건 진짜"로 읽게 만들었다. 오탐에 신뢰를 덧입힌 셈.

같은 클래스가 세 곳에서 반복됐다: unknown 관용을 보조 검증(GSB 키) 예시만 보고 **핵심 검증(L2 내용)까지** 확대해 "🟢 안정 — L2 ✓" 오보를 만들었고, 위임 L2 교체를 `results[-1]`로 해서 version_url이 있는 사이트에서 **L3를 지웠다**. 교훈: **주어진 재현 예시는 결함의 한 표본일 뿐, 결함의 정의가 아니다** — 수리 전에 "이 결함의 변종 축이 뭔가"(회전 방식·검증 종류·층 조합)를 먼저 열거해야 한다. 오답 15호(변종 축)·16호(버전 축)와 같은 뿌리이며, 이번엔 **내가 그 교훈을 알고도 놓쳤다**.

## ⚠️ 오답 20호 — 회귀 테스트가 수리의 가정만 고정

13차 수리에 붙인 클로킹 회귀 테스트는 스텁으로 "재확인에서 스팸이 사라지는 경우"를 단언했다. 즉 **수리가 옳다는 가정 자체를 테스트로 박은 것**이고, 순차 회전은 검사 범위 밖이었다. 오답 14호(vacuous 테스트)의 재발. 교정: 테스트를 **실서버**로 교체(`_RotatingHandler` — 주기 2·3 순차 회전 + 진짜 클로킹 대조)하고, 음성 대조로 옛 방식이 RED가 되는 것을 실측 확인.

## 수리 중 자기 적발 (기록)

P3-7(하드FAIL 패스에 낡은 결과 승계 금지)을 넣자마자 기존 회귀 테스트가 RED. 조기 반환이 승계 *주입*뿐 아니라 *기록*까지 건너뛰어, L4 FAIL이 저장되지 않고 13차 P1-1(거짓 복구)이 되살아났다. 주입만 막고 기록은 유지하도록 정정 — **수리發 회귀를 회귀 테스트가 즉시 잡은 사례**(트리클 사가에서 7건 겪은 그 패턴이 이번엔 커밋 전에 걸렸다).

## 검증

76개 5연속 GREEN(9.2~9.5초) · 모듈 단위 실행 복구 확인 · **음성 대조**(옛 재표본 방식 → 순차 주기2 정확히 RED) · 라이브 5층 🟢 · 전 파일 500라인 이하.

## 적용 메모리/규칙

증상 vs 클래스(주어진 예시 = 표본, 변종 축을 먼저 열거) · 음성 대조 필수 · 회귀 테스트는 실서버로(스텁은 가정을 고정) · 보안 조임이 가용성/정확성을 깨는지 양방향 확인 · 500라인 규칙
