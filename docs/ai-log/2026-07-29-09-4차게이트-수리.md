# 2026-07-29 세션 09 — 4차 게이트 → 스레드 누수 P1 수리

**도구**: Claude Code (Claude Fable 5) + Codex (gpt-5.6-sol xhigh)

### ⚠️ 오답 6호 — 동시성 수리가 연 새 결함 표면 + 그걸 통과시킨 내 테스트

3차 G3 수리(총 시한을 스레드로 강제)가 P1을 만듦: future 타임아웃은 실행 중인 워커를 취소하지 못해 **시한 초과마다 스레드·소켓이 누적**되고, 비데몬 워커 탓에 프로세스가 응답이 끝날 때까지 안 죽음(99.7초 실측 — cron 걸면 프로세스 중첩). 더 아픈 건 **내가 만든 실경로 테스트가 이걸 GREEN으로 통과시킨 것** — "예외를 시한 안에 던지는가"만 재고 "뒷정리가 됐는가"를 안 쟀다. 교훈 2개: ① 스레드를 쓰는 수정은 정리 경로까지가 수정이다 ② 테스트는 주장하는 성질 전부를 재야 한다.

### 검사자 오답 2연속 — H-G

3차 러너 권고(반쪽 설정=기동 차단)가 가용성 원칙과 충돌함을 4차 러너가 시인. 2라운드 연속 "검사자 권고도 검증 대상" 사례 축적.

### 수리 (P1 1·P2 1·P3 5 전량) + 결정 2건

- H-A(**결정: 근본안**): 데몬 스레드 + 시한 초과 시 메인에서 `resp.close()`로 블록된 read를 깨워 워커 즉시 회수. 정리 검증(스레드 수 복귀) 테스트 동봉
- H-G(**결정: 완화**): 차단 대신 큰 경고+콘솔 모드 — 사이트 오류 스킵과 동일한 가용성 원칙으로 일관
- H-B 빈 마커 차단 / H-C pending_missed로 사후 보고도 전달까지 재시도 / H-D 엔트리 값 타입 검사 / H-E 주석-현실 정합(시한 초과=장애로 본다 명시)
- 스위트 28→30개 GREEN · 라이브 3층 GREEN

### 적용 메모리/규칙

동시성 도입=정리 경로까지 검증 · 테스트는 주장 성질 전부 단언 · 원칙(가용성) 일관성 감사 · 게이트 반복
