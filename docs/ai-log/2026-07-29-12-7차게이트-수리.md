# 2026-07-29 세션 12 — 7차 게이트 → M-C 수리 무효 정정 + README + PASS 준비

**도구**: Claude Code (Claude Fable 5) + Codex (gpt-5.6-sol xhigh)

### ⚠️ 오답 11호 — "수리 완료"라는 내 보고가 거짓이었다

6차에서 M-C를 "수리 완료"로 보고했지만, 7차 게이트가 재현으로 반증 — 내 가드는 "이미 대기 중인 사후 보고의 덮어쓰기 방지"만 했고, 정작 문제였던 "이탈 구간에서 최악(FAIL) 사유 보존"은 안 됐다(OK→FAIL미발송→WARN미발송→OK에서 WARN 사유가 인용됨). **가드를 추가했다는 사실과 문제가 풀렸다는 사실은 다르다** — 수리 보고도 시퀀스 재현으로 검증해야 함을 확인. 이번엔 이탈 구간 최악 사유를 별도 누적(`unnotified_worst`)하는 구조로 실제 수리하고, 정확히 그 시퀀스를 회귀 테스트로 고정.

### ⚠️ 오답 12호 — docstring의 성립하지 않는 "보장"

"최종 회수 보장은 워커 자신의 read timeout"이라 썼지만, 데이터가 계속 흐르는 트리클에서는 read timeout(무응답 타이머)이 영원히 안 걸림 — 실측 워커 2→4→6→8 선형 누적. 수리: 워커 인라인 데드라인 자가 종료 복원(데이터가 흐르는 동안엔 청크마다 시한 검사) + "보장" 표현 삭제, 병적 트리클 잔여 한계는 README에 정직 명시.

### 결정 2건

- N-A: 문서화만 vs 코드+문서 → **병행** (흔한 경로는 자가 종료로 실제로 닫고, 못 닫는 병적 경로만 한계로 명시 — "전부 한계"로 도망가지 않음)
- N-B: README **직접 작성** — 3줄 인트로/설치·실행/설정 예시/알림 정책/알려진 한계 4항/과정 기록 안내. checks.py의 "README 참조"가 실존 문서를 가리키게 됨

### 검증

반복 5회 전부 **33/33 GREEN** · 라이브 3층 GREEN · N-D(version_url 스킴) 스윕 포함. 8차에 PASS 판정 요청.

### 적용 메모리/규칙

수리 보고도 재현 검증 대상(오답 11호) · 문서의 "보장"은 실측 책임을 동반 · 한계는 숨기지 않고 명시 · 반복 실행 검증
