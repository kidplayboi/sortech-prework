# 2026-07-29 세션 11 — 6차 게이트 → 테스트 신뢰성·클래스 스윕 수리

**도구**: Claude Code (Claude Fable 5) + Codex (gpt-5.6-sol xhigh)

### ⚠️ 오답 9호 — "GREEN"이라는 보고 자체가 재현 불가였다

내가 "32개 GREEN"으로 보고한 스위트가 러너의 **반복 실행 19회 중 2회 실패**(CPU 부하 시 17%). 코드는 정상이고, 비동기 best-effort 회수에 동기 수렴을 단언한 테스트가 결함. 교훈: **단발 실행 GREEN은 증거로 부족하다** — 동시성 테스트는 반복 실행으로 확인해야 한다. 이번 수리 검증부터 5연속 실행으로 전환했고, 그 과정에서 문구 변경이 다른 테스트 단언을 깨뜨린 것도 즉시 검출됨.

### ⚠️ 오답 10호 — 클래스 스윕 누락

`load_state`는 3차에 "유효 JSON이어도 형태 검증"까지 하드닝해 놓고, **정확히 같은 클래스인 `load_sites`는 방치**(깨진 설정 = 트레이스백 크래시). "같은 구조의 다른 인스턴스를 전수 확인하라"는 원칙을 스스로 어긴 실례로 기록.

### 수리 (P2 2·P3 6 전량) + 결정 2건

- M-A(**결정: 이름표 추적**): 워커 스레드에 `watcher-*` 이름 부여, 테스트는 그 스레드만 추적(전역 카운트는 무관 스레드에 오염). 수렴 창 8초
- M-D(**결정: 러너 문안 채택**): "🟠 이전 순단 사후 보고(현재 상태 아님) — …"
- M-B load_sites 방어 / M-C 최악 이벤트 보존 / M-E name·url 스킴 검증 / M-F 캐치업 버스트 방지 / M-G 스테일 주석 정정 / M-H 지속시간 스냅샷

### 검증

**반복 5회 전부 32/32 OK** · 라이브 3층 GREEN. 프로덕션 코드 기준 이번 라운드 신규 P1 0·P2 1 — 6라운드 중 가장 깨끗(러너 평).

### 적용 메모리/규칙

동시성 테스트=반복 실행 검증 · 증상 아닌 클래스 전수 스윕(오답 10호로 재확인) · 문구 변경도 회귀 원인
