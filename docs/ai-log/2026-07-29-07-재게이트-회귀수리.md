# 2026-07-29 세션 07 — 2차 게이트 FAIL(수리發 회귀) → 회귀 수리 + 테스트 고정

**도구**: Claude Code (Claude Fable 5, 작성자·수리) + Codex (gpt-5.6-sol xhigh, 검사자)

### ⚠️ 오답 4호 — "수리했다"가 새 결함을 만들었다

2차 게이트 결과: 1차 지적 12건은 전부 종결 확인됐지만, **1차 수리가 회귀 4건을 새로 만듦** (N1~N4, 상세=`docs/audits/2026-07-29-codex-gate-D1.md`). 가장 아픈 N1: 총 시한 가드(P2-7 수리)를 넣으면서 잘린 본문을 정상 응답처럼 반환 → 느리지만 멀쩡한 사이트를 "핵심 내용 없음"으로 오탐 — **1차 P1-1과 같은 증상 클래스가 다른 경로로 부활**. 교훈이 명확함: 회귀 2건이 정확히 **테스트 없는 구간**에서 났다. "고쳤다"는 주장도 검증 대상이며, 수리는 반드시 그 자리에 테스트를 남겨야 한다.

### 사람 몫 결정 2건 (게이트가 물은 것 → 내 판단)

- N3 곁가지: 설정 오류 시 전체 기동 차단 유지 vs 해당 항목만 스킵 → **가용성 우선으로 스킵** (감시 도구가 오타 한 줄에 통째로 멈추는 게 더 나쁨. 전멸 시에만 차단)
- N9(cp949 콘솔): 평가자에게 환경변수 요구 vs 코드가 견디게 → **stdout utf-8 reconfigure로 코드가 흡수**

### 수리 + 검증

- N1~N4 + P3 7건 전량 수리 (truncated 플래그·confirmed_reason 분리·격리 범위 전체+타입 검증·콘솔=전달·markers 리스트 강제·스키마 잔여 키 제거·gitignore tmp·duration 0 문구·utf-8 콘솔·with resp)
- **회귀 4건을 테스트로 고정** — 스위트 16→22개, 전부 GREEN
- 라이브: status 읽기 전용 GREEN · once 3층 GREEN (`L1 HTTP 200 · L2 마커 2/2 · L3 1.0.1=1.0.1`)
- 3차 게이트 요청

### 적용 메모리/규칙

수리도 검증 대상(오답 4호로 재확인) · 회귀는 테스트로 고정 · 가용성 vs 보안 트레이드오프 명시 결정 · 게이트 반복 = P1/P2 0까지
