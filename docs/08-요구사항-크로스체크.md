# 요구사항 전항목 크로스체크 (S5)

> 원본 = `docs/00-과제-요구사항.md` (과제 docx를 XML 레벨로 전수 파싱해 추출).
> 각 항목마다 **충족 여부 + 근거 파일/수치**를 단다. 근거 없는 ✅는 쓰지 않는다.
> 최종 갱신 = 2026-07-31

## 과제 요구 (T)

| ID | 요구 | 상태 | 근거 |
|----|------|------|------|
| T1 | 반복적/귀찮은 일 하나를 자동 처리하는 **작은** 프로그램 | ✅ | 배포 후 "진짜 반영됐나"를 눈으로 확인하던 일. 코드 `watcher/` 12개 파일·2,128줄 + 테스트 6개 파일·1,539줄 (2026-07-31 실측) |
| T2 | **실제로 동작** | ✅ | 라이브 감시 대상 https://kidplayboi.github.io/deploy-watcher-demo/ 에 대해 5개 층 실측 GREEN. 텔레그램 실채널 발송 검증 완료. S4 녹화로 재확인 |
| T3 | 핵심 기능 **1가지**면 충분 | ✅ | 핵심은 하나 — **"배포한 게 사용자 화면까지 실제로 반영됐는가"**. 5개 층은 그 하나를 서로 다른 각도에서 확인하는 장치이지 기능 5개가 아니다 |
| T4 | 예외 처리·사용성 개선 1~2가지 (선택) | ✅ 초과 | 요청 총 시한·본문 크기 상한(병적 서버가 순찰 전체를 점유하는 것 차단) · 설정 오류는 해당 사이트만 제외하고 감시 계속(가용성 우선) · 상태 파일 손상 시 자가 복구 · 전송 실패는 상태 유지로 자연 재시도 |
| T5 | 겹치는 주제 회피 · 본인 **실제 반복 작업**에서 발굴 | ✅ | 문서가 경고한 "채용공고 수집" 계열 아님. 실제로 겪은 사고에서 출발 — 배포했는데 CDN 캐시 때문에 사용자만 옛 화면을 보고 있었고, 200 OK만 보고 정상으로 넘어갔다 |
| T6 | 언어·도구 자유 | ✅ | Python 3.13 · 외부 의존성은 `requests`/`urllib3`, 렌더 확인에만 Playwright(선택 의존성) |

## 제출물 (S)

| ID | 제출물 | 상태 | 근거 |
|----|--------|------|------|
| S1 | **AI 활용 기록** (docx/pdf) | ✅ | `제출물/S1-AI활용기록.docx` |
| S1a | 사용 AI 도구 목록 (모델명까지) | ✅ | S1 §1 — Claude Opus 5 / Claude Fable 5 / Codex gpt-5.6-sol(reasoning xhigh) / 웹리서치 에이전트. **어디에 무엇을 썼는지까지** 명시 |
| S1b | 실제 주고받은 대화 발췌 (**날것 환영**) | ✅ | `docs/ai-log/raw/00-지시-전문.md` — 세션 트랜스크립트에서 **사람이 실제로 친 말 42턴**을 시간순 원문 추출(요약·각색 0). 추출기 = `tools/extract_log.py`, 마스킹 후 차단형 감사 통과 |
| S1c | **AI가 틀린 순간 + 바로잡은 방법** ★필수 | ✅ | 오답 **34건**을 발생 즉시 박제. S1 §3에 그중 구조적 통찰 8건을 "프롬프트 → AI 답변 → 나의 대응" 서사로 전개, 나머지는 `docs/ai-log/` 25개 세션 파일 |
| S2 | **회고** (docx/pdf) | ✅ | `제출물/S2-회고.docx` |
| S2a | 어떤 도구를 어떤 순서로 | ✅ | S2 §1 |
| S2b | 막힌 지점 + AI로 어떻게 풀었나 | ✅ | S2 §2 |
| S2c | AI 답이 틀리거나 마음에 안 들 때 대응 | ✅ | S2 §3 |
| S2d | 다시 한다면 무엇을 다르게 | ✅ | S2 §4 |
| S3 | 코드 (GitHub 링크) | ✅ | https://github.com/kidplayboi/sortech-prework — 커밋 히스토리 자체가 과정 증거 |
| S4 | **실행 화면 녹화본** | 🔴 미완 | 하네스는 완성(`docs/demo/`, 한 줄 실행 3분 자동). 촬영은 깨끗한 데스크톱이 전제라 마지막에 수행 |
| S5 | 맨 앞 3줄 인트로 | ✅ | S1·S2 양쪽 맨 앞 + 메일 본문 |

**형식 요구** — "단순 프롬프트 목록 ❌ → 프롬프트 → AI 답변 → 나의 대응 서사 ⭕":
S1 §3의 8건 전부 이 3단 구조로 작성. 각 건마다 **그때 실제로 돌린 검증 명령과 출력**을 붙였다.

## 평가 포인트 (E)

| ID | 항목 | 어떻게 대응했나 |
|----|------|----------------|
| E1 | 문제 발견 | 문서가 경고한 흔한 주제를 피하고, 실제로 당한 사고(배포 반영 확인 누락)에서 출발. 도구가 아니라 **"200 OK는 배포 성공이 아니다"**라는 관찰이 출발점 |
| E2 | AI를 도구로 잘 부리는가 | ① 문제를 슬라이스로 쪼개 한 번에 하나씩 ② 작성자(Claude)와 **검사자(Codex)를 분리**한 교차 게이트 20라운드 ③ 같은 결함에 프롬프트 형태만 바꿔 검출률을 잰 **통제 실험** (`docs/07-AI검사자-통제실험.md`) |
| E3 | 검증과 판단 | 게이트 지적을 **맹목 수용하지 않고** 각각 수용/기각 판단 + 기각은 `file:line` 근거. 내 수리가 뒤집힌 라운드가 4회 있고 전부 기록. 회귀 테스트 **101개**, 음성 대조(가드를 실제로 떼서 RED 확인)까지 |
| E4 | 문제 해결 과정 | 같은 오탐이 5라운드 반복되자 수리를 정교화하는 대신 **판정 축 자체를 교체**(`docs/06-L5-판정-재설계안.md`). 실측 = 회전 오탐 86/1206 → 0 |
| E5 | 소통 | 못 하는 것을 README '알려진 한계'에 수치와 함께 명시(탐지 하한을 계산식+실측 범위로). 매 세션 기록을 실시간 작성 |

## 행정 (A)

| ID | 항목 | 상태 |
|----|------|------|
| A1 | 제출처 juran03@sortech.co.kr | ⬜ 발송 대기 |
| A2 | 제출 기한 | 2026-08-02 (한국시간) |
| A3 | 면접 창원지사 원칙 | 확인함 |
| A4 | 근무지 — 신입 창원 고정 / 경력 협의 | **AI Native 개발자 경력직 지원**. 근무지는 **경기지사(성남시 분당구 수내동)** 가능 조건으로 확인 요청 — 메일 본문에 명시. ⚠️과제 원문은 "경력=협의"까지만 적혀 있고 경기지사 언급은 없다(원문 근거 없음 — 지원자 측 조건 제시) |

## 남은 것

| # | 항목 | 담당 |
|---|------|------|
| 1 | S4 실행 화면 녹화 (무편집 3분 원테이크) | 사람 — 깨끗한 데스크톱에서 `docs/demo/run-and-record.ps1` 한 줄 |
| 2 | 메일 발송 | 사람 확인 후 |
