# HANDOFF — 세션 재개용 (컴팩 대비)

> 재개 절차: **이 문서 → `MASTER-TRACKER.md` → `01-스펙.md`** 순서로 읽는다.
> 재개 직후 검증: `python -m unittest discover -s tests`(40개, 동시성 확인은 반복 실행) + `python -m watcher status`

## 스냅샷 (2026-07-30)

- **D1 완료**: 감지 L1~L3(생존/내용/배포반영) + 상태·알림 엔진(관측/통보 분리, 플랩 방지, 순단 사후 보고) + CLI(`status`·`once`·`watch`) + 테스트 40개 + README 완비
- **Codex 교차 게이트 8라운드 PASS** (P1 0 · P2 0) — 판정·수리·결산 전문 = `docs/audits/2026-07-29-codex-gate-D1.md`
- 감시 대상 데모 사이트 라이브: https://kidplayboi.github.io/deploy-watcher-demo/ (v1.0.1, 별도 공개 레포)
- 텔레그램 실채널 연결·발송 검증 완료 (자격 정보는 로컬 `.env` 전용 — 레포 미포함, 형식은 `.env.example`)

## 불변식 (재개 시 반드시 유지)

1. **기록 실시간** — 세션마다 `ai-log/` 파일 1개, "프롬프트→AI 답변→나의 대응" + 적용 메모리/규칙 명시. ⚠️오답은 발생 즉시 박제 (**현재 15호까지** — 제출물의 핵심 자산)
2. **공개 레포 가드** — 시크릿·개인정보·무관 프로젝트 정보 유입 금지
3. **검증 규율** (게이트 8라운드에서 확립): 수리 보고는 시퀀스 재현으로 검증 / 동시성 테스트는 반복 실행 / 같은 텍스트·구조 수정은 grep 전수 / "보장" 서술은 실측 책임 동반
4. **게이트**: 슬라이스 완료마다 Codex 교차 게이트(모델 핀 **gpt-5.6-sol + xhigh** — 평문 gpt-5.6은 400 차단), P1/P2 0까지 반복. 맹목 수용 금지
5. 파일 500라인 금지 · 한 파일 한 책임 · 작업 단위 커밋 + 푸시 후 원격 해시 대조

## D2 확정 스코프 (2026-07-30 회의 확정)

| # | 항목 | 비고 |
|---|------|------|
| 0 | **N-A 수리 + 9차 게이트** (회의 결정 — 문서화 종결안 기각) | 트리클 워커 회수를 실제로 고친다 (read1 기반 즉시 반환 읽기). 수리 후 9차 게이트로 검증 |
| 1 | L4 렌더링 검증 (Playwright 헤드리스) | **선행 조건: "취소 불가 in-flight 작업" 정리 경로 먼저 설계** (게이트 carryover — L4는 느려서 누수 패턴 증폭) |
| 2 | 배포 집중 모드 `watch deploy <site> [--expect "문구"]` | 15초 간격 연속 검증 → 연속 정상 시 안정 판정. **기대 버전 앵커 포함** (게이트 기각-2에서 D2로 승계된 계약) · `--expect` = 빌더형(아임웹 등) 블랙박스용 |
| 3 | **상태보드 = 정적 HTML 한 장** (회의 확정) | `watch` 매 패스마다 board.html 갱신, 서버 없음. meta refresh로 자동 새로고침 |
| 4 | 실행 녹화 (S4 필수) — **Before 오프닝 + 씬 5개 + 클로킹 씬** (회의 확정+L5) | 수동 확인 루틴 10~20초 오프닝 후 씬 구성(`01-스펙.md`) |
| 5 | **L5 보안 축** — UA(+referer) 클로킹 감지 + Safe Browsing **v5** 등재 체크 | 2026-07-30 리서치 후 확정(근거=`03-보안축-리서치.md`). 스크립트 인벤토리 diff는 로드맵 문서화만. ⚠️클로킹 데모 타깃은 GitHub Pages(정적)로 불가 — UA 분기 가능한 무료 동적 엔드포인트 별도 구성 |

## 회의 결정 (2026-07-30)

1. 상태보드 = **정적 HTML 한 장** (서버 모드 없음 — 게이트 표면 최소화)
2. 녹화 = **Before 오프닝 + 씬 5개** 스펙 그대로
3. 제출물 docx = **풀 분량** — 13개 세션 로그 전량 수록 (30쪽+). ⚠️수록 시 토큰·개인정보 마스킹 전수 필수
4. N-A = **9차 게이트로 수리 요구** — "문서화 종결" 판정 기각. 트리클 한계를 실제 수리 후 게이트 재검증
5. (회의 후 추가) **L5 보안 축 = D2 포함** — "여기서 깊게 들어간다" 방향 전환. 리서치 검증 후 결정. GSB API 키 발급 = 사토시(구글 콘솔 로그인 필요)

## 게이트 carryover (D2 착수 시 필독)

- 예외→상태 매핑을 공통화해 "예외가 알림 경로에서 이탈"(G2) 패턴 재발 방지
- 비동기 정리 검증 테스트는 상한/이름표 단언 (M-A 교훈 — 동기 수렴 단언은 필연적 flaky)
- 수용된 한계 = README '알려진 한계' 4항: CDN 쿼리스트링 · K-B 헤더 정체 · 텔레그램 총시한 · TLS-in-TLS(10차에서 추가 — shutdown API 부재). 몰래 재개봉 금지, 필요 시 명시 재논의. 본문 트리클 항목은 회의의 명시 재논의를 거쳐 9~11차에서 수리·삭제됨

## 남은 단계 (MASTER-TRACKER 5~10)

D2 빌드(+게이트) → 녹화(S4) → AI 활용 기록 정제→docx/pdf(S1) → 회고 정제→docx/pdf(S2) → 3줄 인트로+요구사항 전 항목 크로스체크(S5, T/S/E/A 대조) → 메일 제출(A1)

## 미결

- 제출 기한 — 수신 메일에서 확인 필요 (외부 정보)
