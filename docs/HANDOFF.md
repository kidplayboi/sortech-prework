# HANDOFF — 세션 재개용 (컴팩 대비)

> 재개 절차: **이 문서 → `MASTER-TRACKER.md` → `docs/06-L5-판정-재설계안.md`** 순서로 읽는다.
> 재개 직후 검증: `python -m unittest discover -s tests`(78개, 동시성 확인은 반복 실행) + `python -m watcher status`

## 스냅샷 (2026-07-31)

- **D1 완료**: 감지 L1~L3(생존/내용/배포반영) + 상태·알림 엔진(관측/통보 분리, 플랩 방지, 순단 사후 보고) + CLI(`status`·`once`·`watch`)
- **D2 코드 완료**: L4 렌더링(Playwright) · `deploy` 집중 모드(`--expect`/`--expect-version`, 종료코드 0/1/3) · 정적 HTML 상태보드 · L5 보안 축(클로킹 + Safe Browsing)
- **Codex 교차 게이트 17라운드** — 8차 PASS(D1 코어) · 12차 PASS(트리클 사가) · **17차 FAIL(P1 0·P2 2·P3 8)**. 전문 = `docs/audits/2026-07-29-codex-gate-D1.md`
- 테스트 78개 5연속 GREEN · 라이브 5층 🟢 실측(2026-07-30 18:19)
- 감시 대상 데모 사이트 라이브: https://kidplayboi.github.io/deploy-watcher-demo/ (v1.0.1, 별도 공개 레포)
- 텔레그램 실채널 연결·발송 검증 완료 (자격 정보는 로컬 `.env` 전용 — 레포 미포함)

## 게이트 현황 = 18·19·20차 전부 처리 완료. 다음 = 남은 제출물(#6~#10)

**20차 판정**: FAIL(P1 1·P2 3·P3 5) → 전건 수리. 이번 라운드 지적은 **코드가 아니라
내 주장**이었다 — "회전 오탐 0"·"하한 1/12"·"하한 아래는 🟠" 셋 다 실측과 달랐다.
뿌리는 내가 만든 측정이 **사이트 시작 회전 위상을 0으로 고정**한 것. 전부 실측치로 정정하고
회귀도 "0"이 아니라 **상한(2%)**을 지키는 형태로 바꿨다. 상세 = 감사 문서 §20차.

⚠️**게이트를 더 돌릴지는 형 판단**: 17→20차가 전부 FAIL이었지만 지적의 층이 올라가는
중이다(결함 → 수리가 만든 결함 → 수리의 비대칭 → 측정·서술 부정확). 제출 기한이 8/2고
#6~#10이 미착수라, 21차는 남은 제출물을 끝낸 뒤 여유가 있으면 도는 것을 권한다.

## (완료) L5 판정 재설계 + 게이트 18·19·20차

**L5 판정 재설계 + 18·19차 게이트 수리 완료(2026-07-31).**

- 구현: `watcher/cloak_decision.py`(순수 판정) + `watcher/security.py`(요청·조립)
- 회귀: `tests/test_cloaking.py` (500라인 규칙에 따라 test_layers.py에서 분리) — 100개
- 실측: 회전 오탐 **86/1206 → 0/1206** · 다중 시드 스윕 0 · 무작위 삽입 3.4~4.1% → 0% ·
  탐지 1/2·1/3·1/4·1/6 전부 확정 · 거짓 초록 0
- **탐지 하한을 계산식으로 남겼다**: 확정은 순증 누적이라 패스당 관측 확률 > 1/2 필요 →
  봇 표본 B(5~12)에서 `r ≳ 1/12`. 1/16·1/20은 🟠 의심에 머문다(README·테스트에 박음)
- 18차 FAIL 처리: P1-1(봇 1/2 미만 노출 확정 불가 — 15차 P3-3 재개방) · P2-1(state 손상 →
  알림 영구 블랙아웃) · P2-2(중복 후보가 임계 반토막) · P3 5건
- 19차 FAIL 처리: **P1-1**(축 3을 봇 블록에 안 걸어 벽이 `2×BOT_SAMPLES=14`로 옮겨졌을 뿐) ·
  P2-1(임계 비단조 — 감쇠가 확정을 만든다) · P2-2(회전 오탐 0이 고정 시드 관측치) ·
  P2-3(반증 경로 도달 불가 + 알림 문구가 관측과 모순) · P3 5건
- 델타·수치 전문 = `docs/06-L5-판정-재설계안.md` **§7** / 감사 = `docs/audits/...gate-D1.md` §18·19차

한 줄 요약: 5라운드 반복 실패의 뿌리는 "한 패스 안에서 표본 배치를 바꾸는" 단일 축이었다.
축을 3개로 분리했다 — ① 회전 감지 ② 패스 간 누적 확정 ③ 위상 무작위화.
마커 소실 형제 경로(P2-2)도 같은 장치를 경유한다.

⚠️**합의 범위 밖 추가 1건(명시)**: 미관측 패스를 리셋이 아니라 감쇠로 처리한다.
간헐 클로커의 🔴→🟢"회복"→🔴 거짓 복구를 막기 위한 것이고, 형에게 보고했다.

## 불변식 (재개 시 반드시 유지)

1. **기록 실시간** — 세션마다 `ai-log/` 파일 1개, "프롬프트→AI 답변→나의 대응" + 적용 메모리/규칙 명시. ⚠️오답은 발생 즉시 박제 (**현재 28호까지** — 제출물의 핵심 자산)
2. **공개 레포 가드** — 시크릿·개인정보·무관 프로젝트 정보 유입 금지
3. **검증 규율**: 수리 보고는 시퀀스 재현으로 검증 / 동시성 테스트는 반복 실행 / 같은 텍스트·구조 수정은 grep 전수 / "보장" 서술은 실측 책임 동반
4. **음성 대조는 테스트 추가 시점에도** — 새 회귀 테스트는 "결함 있는 코드에서 실제로 RED가 되는가"를 먼저 확인한다 (17차 교훈 ②: 주기 12 테스트가 실패 불가 구조였다). **대조 장치 자체도 검증한다** — 상수를 기본 인자로 박으면 패치가 안 먹어 대조가 아무것도 제거하지 않는다 (오답 27호)
5. **계측기를 먼저 의심한다** — 프로브 결과가 코드 변경 방향과 어긋나면 별도 프로세스로 재현부터. 임시 포트 고갈이 만든 `unknown`을 판정으로 집계하면 없는 문제를 만든다 (오답 28호)
5. **게이트**: 슬라이스 완료마다 Codex 교차 게이트(모델 핀 **gpt-5.6-sol + xhigh** — 평문 gpt-5.6은 400 차단), P1/P2 0까지 반복. 맹목 수용 금지
6. 파일 500라인 금지 · 한 파일 한 책임 · 작업 단위 커밋 + 푸시 후 원격 해시 대조

## 녹화(S4) = **맨 마지막**, 깨끗한 데스크톱에서 (2026-07-31 확정)

하네스는 완성됐다(`docs/demo/` — 형이 한 줄 실행하면 3분 자동). 다만 **순서를 맨 뒤로 뺀다**:
녹화는 화면의 사각형을 찍는 것이라 그 자리에 있는 게 다 찍힌다. 리허설에서 다른 프로젝트
작업 세션과 메신저 창이 그대로 찍혔고 즉시 폐기했다. 재부팅해서 화면을 비운 뒤 찍는다.
가드: `run-and-record.ps1`은 자기 창이 최상위일 때만 녹화하고 아니면 중단(바탕화면 대체 없음).

## 확정 사항 (2026-07-31 형 승인)

| # | 결정 | 비고 |
|---|------|------|
| 1 | **일정 3일** — 7/31 재설계+18차 게이트 → 8/1 AI 검사자 실험+녹화 → 8/2 docx 2종+크로스체크+제출 | |
| 2 | **녹화 = 무편집 3분 원테이크** + **PDF 보조자료 병행** | 원문 근거: 요구사항에 "(실행 화면 녹화본 첨부)" 명시 · FAQ가 평가자의 "화면 녹화" 시청을 전제. 단 공식 제출물 3가지 목록에는 없음(부속 첨부물) |
| 3 | **기능 추가 금지** — 남은 작업은 수리·기록·제출물뿐 | |
| 4 | **실제 AI 대화 원문 추출 필수** | S1b가 "날것 그대로 환영"인데 현 `ai-log` 23개는 전부 요약 서사체(389줄) — 날것 0줄. 세션 트랜스크립트 원본에서 추출 |
| 5 | **요구사항 크로스체크 빠짐없이** | T/S/E/A 전 항목 대조 (S5) |

## 다음 작업 순서

1. ~~L5 재설계 시공~~ ✅ 2026-07-31
2. ~~자가검증 (주기 전수 스윕 · 음성 대조 3종 · 탐지력 보존 · 픽스처 교정)~~ ✅ 2026-07-31
3. ~~18·19차 게이트~~ ✅ 둘 다 FAIL → **전건 수리 완료**. 다음 = **20차 재게이트**
   (19차 수리 델타 검증. 러너 carryover: 봇 블록 축 3 적용의 실효 확인 · 임계 통일 후
   단조성 · 다중 시드 잔존율 · 탐지 하한 1/12 모델의 실측 일치 · 범위 밖 P3 4건
   = GSB shape(수리됨)·save_state 격리·deploy 시작알림·L4 승계 라벨링)
4. 17차 P3 잔여 정리 (README 한계·빌드노트 스테일·죽은 인자·Playwright 선택 의존성 선언·deploy docstring·max-wait 문구)
5. **AI 대화 원문 추출 파이프** (마스킹 전수 — 토큰·개인정보)
6. **AI 검사자 통제 실험** — 같은 결함에 프롬프트 형태만 바꿔 검출률 비교(17차 렌즈 3종 기록이 씨앗)
7. 녹화(S4) → S1 docx → S2 docx → 3줄 인트로+크로스체크(S5) → 제출(A1)

## 게이트 carryover (필독)

- 예외→상태 매핑 공통화로 "예외가 알림 경로에서 이탈"(G2) 재발 방지
- 비동기 정리 검증 테스트는 상한/이름표 단언 (M-A — 동기 수렴 단언은 필연적 flaky)
- 수용된 한계 = README '알려진 한계' 4항: CDN 쿼리스트링 · K-B 헤더 정체 · 텔레그램 총시한 · TLS-in-TLS. 몰래 재개봉 금지
- **17차 신규 carryover**: 축 2·3 도입 시 P2-2(마커)·P3-1(랜덤 오탐)이 함께 닫힌다 — 증상별 패치로 가면 6라운드째 같은 반증이 온다
- 픽스처 `/rotate12` 잔여류 `{1,5,9}`는 15차 결함용 음성 대조로 유지하고, 주기 12 커버리지용 **단일 잔여류 `{1}`** 픽스처를 별도 추가(현 코드에서 반드시 RED여야 함)

## 일정

**제출 기한 = 2026-08-02 (한국시간)** · 실작업 = 7/31·8/1·8/2
